# Changes Tracker

| # | Timestamp | File | Previous | New | Issue | Cause | Fix |
|---|-----------|------|----------|-----|-------|-------|-----|
| 1 | 2026-04-23 10:38 | backend/routes/geofence.py | 123 LOC route file with 5 endpoints | DELETED | Dead code — zero frontend calls, zero backend imports | Geofence feature scaffolded for future GPS but never wired in | Removed entire file |
| 2 | 2026-04-23 10:38 | backend/main.py | Imported + registered geofence_router | Lines removed | Route registration of dead module | Geofence file deleted | Removed `from .routes.geofence import router as geofence_router` and `app.include_router(geofence_router)` |
| 3 | 2026-04-23 10:38 | backend/database/models.py | Geofence model class (15 LOC at line 384) | DELETED | Dead model — only referenced by deleted geofence route | Geofence route deleted | Removed Geofence class |
| 4 | 2026-04-23 10:38 | backend/database/models.py | LocationQueue, TripPattern, CostConfig, PerformanceScore, Achievement, AnomalyAlert, SafetyChecklist, SafetyIncident, AuditLog (~120 LOC, lines 428-547) | DELETED | Dead models — zero queries anywhere in backend or frontend | Schema-only stubs never wired into business logic | Removed 9 model classes; tables remain in DB if previously created (no migration to drop) |
| 5 | 2026-04-23 10:38 | backend/utils/analytics_helpers.py | (did not exist) | NEW 18 LOC | Duplicated `get_config()` in 4 files + `get_avg_capacity()` in 2 files | Helpers copy-pasted instead of shared | Created shared module with both helpers |
| 6 | 2026-04-23 10:38 | backend/routes/daily_plans.py | Inline `get_config` (3 LOC) | Imports from analytics_helpers | Duplication | See #5 | Replaced with `from ..utils.analytics_helpers import get_config` |
| 7 | 2026-04-23 10:38 | backend/routes/statistics.py | Inline `get_config` + `get_avg_capacity` (12 LOC) | Imports from analytics_helpers | Duplication | See #5 | Replaced with shared imports |
| 8 | 2026-04-23 10:38 | backend/routes/performance_analytics.py | Inline `get_config` + `get_avg_capacity` (12 LOC) | Imports from analytics_helpers | Duplication | See #5 | Replaced with shared imports |
| 9 | 2026-04-23 10:38 | backend/routes/plans.py | Inline `get_config` (3 LOC) | Imports from analytics_helpers | Duplication | See #5 | Replaced with shared import |
| 10 | 2026-04-23 10:38 | CLAUDE.md | Geofence model + API endpoints documented | Removed | Stale docs reference deleted code | Cascading update | Removed `Geofence` from key models list and `Geofences` API section |
| 11 | 2026-04-23 10:55 | backend/routes/trip_crud.py:20 | `from ..utils.soft_delete import active_only` | `from ..utils.soft_delete import active_only, soft_delete` | `NameError: name 'soft_delete' is not defined` at line 789 broke `test_delete_pending_trip` | Missing symbol in import list — `soft_delete()` called but never imported | Added `soft_delete` to import |
| 12 | 2026-04-23 10:55 | ~/.claude/settings.json:161 | `code-review-graph update --quiet` | `code-review-graph update` | PostToolUse hook errored on every Edit/Write/Bash | `--quiet` flag not supported by `code-review-graph update` subcommand | Removed `--quiet` flag (effective next session) |
| 13 | 2026-04-23 10:50 | git | Working tree had uncommitted changes | Committed `0918ef55` to `feature/strip-security-to-local-auth`, pushed to origin | Need to persist tracked refactor changes | User approved Phase A | Committed only tracked files (main.py + models.py); untracked route edits stay on disk per branch convention |
| 14 | 2026-04-23 11:10 | frontend/src/pages/AdminPlanning.jsx | 511 LOC page component | DELETED | Dead code — zero imports, not in App.jsx routes | Superseded by MonthlyPlanning.jsx but never removed | Removed file |
| 15 | 2026-04-23 11:10 | frontend/src/pages/LiveOperations.jsx | 787 LOC page component | DELETED | Dead code — zero imports, not in App.jsx routes | Operations.jsx serves same purpose; LiveOps was duplicate | Removed file |
| 16 | 2026-04-23 11:10 | frontend/src/pages/PlanningHistory.jsx | 325 LOC page component | DELETED | Dead code — zero imports, not in App.jsx routes | PlanHistory component (in MonthlyPlanning) replaced this | Removed file |
| 17 | 2026-04-23 11:10 | frontend/src/App.css | 42 LOC | DELETED | Dead — zero imports, no JS reference | index.css used instead, App.css orphaned | Removed file |
| 18 | 2026-04-23 11:10 | CLAUDE.md | LiveOperations.jsx documented in pages list | Removed | Stale doc reference | Cascading update | Removed bullet from `frontend/src/pages/` list |
| 19 | 2026-04-23 11:15 | backend/alembic/versions/h1i2j3k4l5m6_drop_dead_tables.py | (did not exist) | NEW migration | 10 orphaned tables in DB after model deletes (#3, #4) | Models removed but DDL tables persisted | Conditional `DROP TABLE` for: geofences, achievements, anomaly_alerts, audit_logs, cost_configs, location_queues, performance_scores, safety_checklists, safety_incidents, trip_patterns. Downgrade no-op |
| 20 | 2026-04-23 11:15 | backend/alembic/versions/h1i2j3k4l5m6_drop_dead_tables.py | down_revision = 'g1h2i3j4k5l6' | down_revision = '3878275160e7' | `Multiple head revisions` error — branched off wrong parent | `drop_sound_alerts_enabled` already chained from `g1h2i3j4k5l6` | Re-parented to `3878275160e7` to merge branches |
| 21 | 2026-04-23 11:15 | DB schema | 10 orphaned tables present | DROPPED | Dead tables waste schema, confuse devs | Migration applied via `alembic upgrade head` | DB head now at `h1i2j3k4l5m6` |
| 22 | 2026-05-06 17:00 | backend/utils/suveechi_sync.py | (did not exist) | NEW 152 LOC | Need live torpedo GPS for live tracking map | SuVeechi MySQL view shared 02-Apr; no integration yet | Pull `vw_unit_status_ist` → upsert FleetLiveLocation + FleetManagement; normalize 'TLC 01' → 'TLC-01'; map status Idle/Moving→Operating, Ign Off→Maintenance; invalidate fleet_cache after each sync |
| 23 | 2026-05-06 17:00 | backend/main.py | scheduler runs WhatsApp daily report only | Added `schedule_suveechi_sync()` | Wire SuVeechi sync to APScheduler interval trigger | Job needed for live map updates | New IntervalTrigger every `SUVEECHI_SYNC_INTERVAL_SECONDS` (default 10s); `asyncio.to_thread` wraps sync pymysql call; daily prune at 02:00 of stale locations >24h |
| 24 | 2026-05-06 17:00 | backend/requirements.txt | no MySQL driver | Added `pymysql>=1.1.0` | SuVeechi sync uses MySQL | New dep | Package added under Caching section |
| 25 | 2026-05-06 17:00 | backend/.env.example | no SuVeechi config | Added 7 vars | Document SuVeechi connection settings | Required for sync feature | `SUVEECHI_SYNC_ENABLED`, `SUVEECHI_HOST/PORT/USER/PASSWORD/DB/VIEW`, `SUVEECHI_SYNC_INTERVAL_SECONDS` |
| 26 | 2026-05-07 09:00 | backend/routes/fleet.py | `GET /api/fleet/live` returned only x/y/fleet_id/type | Extended response with `status`, `capacity`, ISO `last_updated` via LEFT JOIN FleetManagement | Frontend needs status to colour markers + show drawer header | Marker colours and drawer status badge depend on this | Added outer-join + ISO-format datetime; cache key/TTL unchanged |
| 27 | 2026-05-07 09:00 | frontend/src/utils/torpedoStatus.js | (did not exist) | NEW 32 LOC | Single source of truth for status→colour and status→short-label maps | Avoid duplicating mapping in marker, drawer, legend | Handles HMD vocabulary (Operating/Assigned/Maintenance) AND SuVeechi vocabulary (Idle/Moving/Ign Off); default slate colour for unknown |
| 28 | 2026-05-07 09:00 | frontend/src/pages/Dashboard.jsx | `createFleetIcon(id)` used hard-coded `hsl(var(--primary))` for all torpedoes; label was just `{id}` | `createFleetIcon(id, status)` — colour from `statusColor()`, label `{id} · {short}` | Operators couldn't distinguish Idle/Moving/Ign Off torpedoes at a glance | Status visibility | Marker box widened to 60px; fleet marker call site passes `fleet.status` |
| 29 | 2026-05-07 09:00 | frontend/src/pages/Dashboard.jsx | Map opened at default centre, user panned/zoomed manually | NEW `FitBoundsOnFleet` helper component + `hasFittedFleet` state | Auto-zoom to all 53 torpedoes on first load | UX improvement | Fits once on first non-empty fleet load with `padding: [60,60], maxZoom: 16`; idempotent via `hasFitted` flag |
| 30 | 2026-05-07 10:55 | frontend/src/components/TorpedoDrawer.jsx | (did not exist) | NEW ~470 LOC | Phase 2 — clicking a torpedo only showed a tiny `"TORPEDO: TLC-16 LIVE TRACKING ACTIVE"` popup, no useful info | Operators needed live position, current/last trip, history, capacity at a glance | 420px right-side slide-in drawer with sticky header (fleet_id + status badge + "updated Ns ago" + ✕) and 5 sections: Live Position (5s poll, derived speed via Haversine), Current Trip (10s poll only when Assigned), Last Trip, Trip History (20 rows, manual refresh), Maintenance & Capacity. Close on ✕/backdrop/Esc. Reuses existing `/api/fleet/live` and `/api/fleet-management/{fleet_id}/details` — no backend change |
| 31 | 2026-05-07 10:55 | frontend/src/pages/Dashboard.jsx | Torpedo `<Marker>` rendered a `<Popup>` with stub text; no drawer state | Added `selectedFleetId` state, replaced popup with `eventHandlers={{ click: () => setSelectedFleetId(fleet.fleet_id) }}`, rendered `<TorpedoDrawer fleetId={selectedFleetId} onClose={...} />` at page level | Wire marker click → drawer | Phase 2 entry point | Imported `TorpedoDrawer`; removed Popup JSX from torpedo marker; drawer mounted as sibling of map container so it overlays full viewport |
| 32 | 2026-05-07 11:05 | frontend/src/components/TorpedoDrawer.jsx | Backdrop `zIndex: 1100`, drawer `zIndex: 1101` | Backdrop `2200`, drawer `2201` | On SMS4 the drawer's sticky header (fleet_id, status badge, ✕) was hidden behind the app's top bar — only the colored top-border stripe peeked through | App header is `z-index: 2000` and sidebar `z-index: 2100` (in `index.css`); my 1101 was below both | Bumped backdrop+drawer z-indices above sidebar so the whole drawer overlays page chrome (standard side-drawer pattern). Verified on SMS4 with screenshot showing `TLC-05 · IDLE · updated 0s ago` header rendering correctly |
| 33 | 2026-05-07 14:08 | frontend/src/pages/Dashboard.jsx | `currentTime` state + `setInterval(setCurrentTime, 1000)`; `planningSummary` state + `/api/daily-plans/dashboard-summary` fetched on mount AND every 5s poll; `error` state + `setError` calls | All three removed. Fetches reduced to `/api/locations` + `/api/weighbridges` (initial) and `/api/fleet/live` only (poll) | **PERF REGRESSION**: SMS4 page showed "This page isn't responding" once SuVeechi started feeding 53 live torpedoes | `currentTime` was set every 1s but never read in JSX — caused 1 Hz Dashboard re-render. Each render rebuilt all 53 torpedo markers (`<Marker icon={createFleetIcon(...)} position={[x,y]}>` produces new icon + new position every render → react-leaflet calls `marker.setIcon()` and `setLatLng()` which replace marker DOM HTML). 53 markers × DOM rebuild × 1 Hz ≈ 100-160 ms main-thread work every second, blocking event loop. Pre-SuVeechi the same code ran but FleetLiveLocation was empty so the loop was free. ESLint had already flagged `currentTime`, `planningSummary`, `error` as unused | Removed all three pieces of dead state + their setters + the 1s interval + the dead `/api/daily-plans/dashboard-summary` poll round-trip. Stops the 1 Hz re-render storm at the source |
| 34 | 2026-05-07 14:08 | frontend/src/pages/Dashboard.jsx | Inline torpedo `<Marker key={...} position={[fleet.x, fleet.y]} icon={createFleetIcon(...)} eventHandlers={{click: () => setSelectedFleetId(...)}} />` — every prop allocated fresh per render | New `FleetMarker = memo(({fleet,onClick}) => {...}, customEquality)` subcomponent with `useMemo` for `icon`, `position`, `eventHandlers`, `useCallback` for `handleClick`. Custom `arePropsEqual` compares `fleet_id`/`x`/`y`/`status`/`onClick` | Defense-in-depth — even when re-renders DO fire (every 5s poll), unchanged torpedoes should skip DOM work | react-leaflet's `<Marker>` triggers `setIcon`/`setLatLng` on prop reference change, even if values identical | `React.memo` skips re-render when no torpedo data changed; internal `useMemo` keeps icon+position references stable across renders. Idle torpedoes (most of the fleet) now incur **zero** DOM work per poll. Combined with `useCallback` on `handleFleetClick`/`handleDrawerClose` in Dashboard so child memos hold |
| 35 | 2026-05-07 14:08 | frontend/src/pages/Dashboard.jsx | `createFleetIcon(id, status)` allocated a fresh `L.divIcon` on every call | Module-level `_fleetIconCache` Map keyed by `${id}|${status}` returns the same divIcon instance for repeat (id, status) pairs | Belt-and-braces — guarantees icon-prop reference stability even outside the `useMemo` path | Function-level memoization | Bounded ~265 entries (53 fleets × ~5 statuses); GC-safe |
| 36 | 2026-05-07 14:08 | frontend/src/pages/Dashboard.jsx | `<TorpedoDrawer onClose={() => setSelectedFleetId(null)} />` — new function reference every render | `const handleDrawerClose = useCallback(() => setSelectedFleetId(null), [])` then `onClose={handleDrawerClose}` | Drawer's Esc-key effect listed `onClose` in deps → tore down + re-added the global keydown listener on every Dashboard render while drawer was open | New closure each render | Stable callback identity prevents listener thrash |
| 37 | 2026-05-07 14:08 | frontend/src/components/TorpedoDrawer.jsx | `tick >= 0 && formatMinutes(elapsedMinutes(currentTrip.assigned_at))` — odd guard hack | `formatMinutes(elapsedMinutes(currentTrip.assigned_at))` (plain) | Code clarity — `tick` triggers re-render at the component level, the `&&` was a no-op | Confused refactor leftover | Removed the guard; behaviour identical |
| 38 | 2026-05-07 14:30 | backend/routes/fleet.py | `/api/fleet/live` subquery used `MAX(last_updated) GROUP BY fleet_id`, then JOINed on `(fleet_id == fleet_id) AND (last_updated == max_updated)` | Subquery now uses `MAX(id) GROUP BY fleet_id`, JOIN on `id == max_id` | **CRITICAL: API was returning ~28,000 rows** (53 torpedoes × hundreds of dupes each) — frontend rendered 28k markers stacked, page locked up | SuVeechi reports the same `reporttime_ist` for an idle torpedo across many sync ticks; sync appended a new FleetLiveLocation row each tick with that same timestamp; the JOIN-on-last_updated then matched ALL of them per fleet_id | `id` is unique + monotonic, so `MAX(id) GROUP BY fleet_id` returns exactly one row per torpedo regardless of timestamp duplicates |
| 39 | 2026-05-07 14:30 | backend/utils/suveechi_sync.py | `db.add(FleetLiveLocation(...))` always — appended every tick, no dedupe | Skip insert when `MAX(last_updated)` for this fleet_id is already `>= reported`; only insert when SuVeechi reports a strictly newer timestamp | Stops the dupe accumulation at the source so the table no longer bloats by 53 rows per 10s for idle torpedoes | Append-only design assumed every tick brought a new timestamp, but SuVeechi's `reporttime_ist` doesn't tick when a unit is idle | Added one `MAX(last_updated)` lookup per fleet_id per tick; cheap with the index that should exist on `fleet_id`. Imported `from sqlalchemy import func` |
| 40 | 2026-05-07 14:30 | backend/utils/cleanup_fleet_location_dupes.py | (did not exist) | NEW one-time cleanup script | After fix #38 the existing ~28k dupes are ignored by the query but still consume disk + slow `MAX(id)` scans | Existing data leakage from the bug | Keeps only the row with `MAX(id)` per `fleet_id`; deletes the rest. Run once via `python -m backend.utils.cleanup_fleet_location_dupes` on SMS4 |
| 41 | 2026-05-07 14:30 | frontend/src/pages/Dashboard.jsx | `pollLiveUpdates` set `setFleetLocations(arr)` directly | Defensive Map-based dedupe by `fleet_id`, keeping the row with newest `last_updated` (highest `id` as tiebreaker), then `setFleetLocations(Array.from(byId.values()))` | Belt-and-braces — never render two markers for the same torpedo even if backend ever leaks a duplicate again | Defense in depth | O(n) once-per-poll dedupe at the client edge |
| 42 | 2026-05-07 14:50 | frontend/src/pages/Settings.jsx | Map Configuration card had Default Zoom + Base Map Style only | Added "Torpedo Status Legend" toggle row with `settings-toggle` switch; persists to `localStorage['hmd_show_torpedo_legend']` (default `true`); dispatches `hmd:settings-changed` window event so Dashboard updates live without refresh | User asked for a toggleable on-map count summary | New feature | New `showTorpedoLegend` state mirroring localStorage; toggle handler writes localStorage + fires the custom event |
| 43 | 2026-05-07 14:50 | frontend/src/pages/Dashboard.jsx | No fleet-status summary on the map | New top-right overlay box (when toggle is ON) showing `Total · Idle · Moving · Trip · Maint` counts with status colors from `torpedoStatus.js` | User wanted at-a-glance count + status breakdown | Same | `torpedoBuckets` `useMemo` over `fleetLocations` maps SuVeechi (Idle/Moving/Ign Off) and HMD (Operating/Assigned/Maintenance) vocab into 4 buckets + total; reusable `legendChip(label, value, color)` helper. Dashboard listens to `hmd:settings-changed` so toggling in Settings updates the map immediately |
| 44 | 2026-05-07 15:30 | frontend/src/pages/Dashboard.jsx | Legend chip layout was at top-right — overlapped the producer/consumer/weighbridge legend (truncating "PRODUCER" on narrower viewports) | Moved to bottom-left of the map (`bottom: 24px; left: 24px`) | Layout collision | Side-by-side overlay boxes don't fit on a regular SMS4 viewport | Cleaner separation: top-left = page title, top-right = node legend, bottom-left = torpedo counts |
| 45 | 2026-05-07 15:45 | backend/utils/wbatngl_capacity_sync.py | (did not exist) | NEW ~210 LOC | **Phase 3** — `FleetManagement.capacity` was hard-coded to 360 MT for every torpedo; real capacities vary (BF3/BF4 ~350-375, BF5 ~300-320 per WBATNGL `NET_WEIGHT` history) | Default seed value | Oracle thick-mode connection (re-using pattern from `db_test_v2.py`), idempotent `_ensure_thick_mode` guard so APScheduler can run it repeatedly, scans `BF3."WB_TRANS_DATA_ITRO"` + `BF5."ZWB_TRANSACTION_DATA_ITRO_B"` with `MAX(NET_WEIGHT) GROUP BY LADLENO`, normalises LADLENO ("TLC 01" / "TLC01" / "TLC-1" → canonical "TLC-NN"), `derive_capacity = clamp(observed × 1.05, [300, 400])` MT, only `UPDATE` if value moved by ≥0.05 MT, per-torpedo log line. `run_once()` is idempotent and safe to call manually |
| 46 | 2026-05-07 15:45 | backend/main.py | Scheduler had only WhatsApp daily report + SuVeechi sync | Added `schedule_wbatngl_capacity_sync()` invoked from `startup_event`. CronTrigger at 03:00 IST daily (after the 02:00 SuVeechi prune); guarded by `WBATNGL_SYNC_ENABLED=true`; runs in `asyncio.to_thread` so the Oracle round-trip doesn't block the event loop | Wire the new sync to APScheduler | Phase 3 schedule | `replace_existing=True, max_instances=1, coalesce=True` matches SuVeechi job conventions |
| 47 | 2026-05-07 15:45 | backend/routes/fleet.py | (no manual trigger for capacity sync) | NEW `POST /api/fleet-management/sync-capacities` (admin/trs only). Runs `wbatngl_run_once()` off-thread, invalidates `fleet_management` + `live_fleet_locations` caches, logs an audit event | Operator/admin shouldn't have to wait until 03:00 to see the backfill take effect | Manual override | `Depends(get_db)` so the audit log entry uses a properly-scoped session; `Depends(require_roles("admin", "trs"))` gates access |
| 48 | 2026-05-07 15:45 | backend/.env.example | (no WBATNGL block) | Added 7 vars: `WBATNGL_SYNC_ENABLED`, `WBATNGL_HOST/PORT/USER/PASSWORD/SERVICE`, `ORACLE_INSTANT_CLIENT_DIR` | Document Oracle connection settings | Required for the new sync | Mirrors structure of the existing SUVEECHI block |
| 49 | 2026-05-07 15:45 | backend/requirements.txt | only `pymysql` for SuVeechi | Added `oracledb>=2.0.0` | New Oracle driver dep | Phase 3 needs Oracle | Thick mode requires Instant Client at the path in `ORACLE_INSTANT_CLIENT_DIR` |
| 50 | 2026-05-07 16:30 | backend/utils/wbatngl_capacity_sync.py | `HEADROOM_PCT=1.05`, `CAP_MAX_MT=400.0`, no SQL filter on NET_WEIGHT | `HEADROOM_PCT=1.0` (no headroom — observed MAX is the capacity, no need to inflate), `CAP_MAX_MT=600.0`, new `SANE_NW_MIN=100.0` + `SANE_NW_MAX=1000.0` constants applied as SQL `WHERE NET_WEIGHT BETWEEN :nw_min AND :nw_max` filter | First Phase 3 run on SMS4 clamped EVERY torpedo to 400 MT — typical observed MAX was 425–485 MT (well above the 400 cap), and one row in `BF3.WB_TRANS_DATA_ITRO` had a 6747 MT junk value (TLC-19) that would have skewed that fleet | Plan's clamp [300, 400] was modeled on a 350-380 MT expected range that didn't match real WBATNGL data (JSW Vijayanagar torpedoes routinely carry ~450 MT NET); the ×1.05 headroom on top of MAX further pushed every result past the cap. SQL filter now drops blatant outliers at source so MAX is robust | Bumped cap to 600 MT (admits heaviest legitimate torpedoes), dropped the headroom multiplier (capacity = observed MAX rounded), and added SQL filter to reject NET_WEIGHT < 100 or > 1000 before MAX. Re-running the sync now should produce varied per-torpedo capacities (e.g. TLC-23: 425 MT, TLC-04: 479 MT, TLC-33: 483 MT) instead of all clamped to 400 |
| 51 | 2026-05-07 17:00 | backend/utils/wbatngl_capacity_sync.py | `SANE_NW_MAX=1000.0` | `SANE_NW_MAX=550.0` | Second SMS4 run showed TLC-43 at observed 665.6 MT (clamped to 600), ~180 MT above the rest of the fleet; user judged it suspicious data, not a heavier-duty torpedo | The 1000 MT upper bound was loose — caught the obvious 6747 MT junk row, but admitted the borderline 665.6 MT row that's 35% above the next-highest legitimate value | Tightened SANE_NW_MAX to 550 MT — gives ~67 MT margin over TLC-33's legitimate fleet maximum (483.4 MT), drops the 665.6 row at SQL level so MAX falls back to TLC-43's next-highest legitimate cast |
| 52 | 2026-05-08 18:15 | backend/utils/wbatngl_trip_sync.py:238-264 | Conflict-update set: `{col: stmt.excluded[col] for col in update_cols}` where `update_cols` excluded `("id", "trip_id", "synced_at")` → `synced_at` only set on INSERT, never on UPDATE | Same `update_cols` exclusion list, but the conflict-update dict now explicitly stamps `set_dict["synced_at"] = func.now()` on every upsert | UI showed `last sync 3:28:06 AM` frozen for hours despite the 60s WBATNGL tick logging `fetched=2 upserted=2 errors=0` every minute — operators thought the sync was broken | `MAX(synced_at)` (read by `_last_sync_at()` in `routes/jsw.py:60`) only advanced when a brand-new `trip_id` was INSERTED. During quiet periods JSW revises existing trips (weight/temp updates) without creating new TRIP_IDs → no insert → `synced_at` for those rows never moves → label looks stuck even though sync is healthy | Bumped `synced_at` to `func.now()` inside the `set_dict` so every upsert (insert OR update) re-stamps it. `func.now()` compiles to `NOW()` on PG and `CURRENT_TIMESTAMP` on SQLite so existing 62 sync/jsw tests still pass |
| 53 | 2026-05-08 18:15 | backend/routes/jsw.py:298-356 | (no manual sync trigger) | NEW `POST /api/jsw/sync-now` endpoint with module-level `_sync_in_flight` guard, runs `wbatngl_trip_sync.run_once()` via `asyncio.to_thread`, invalidates `CACHE_KEY_DASHBOARD` after success | The JSW tab's Refresh button only re-read the local mirror — operators expected it to "go fetch from JSW now" but it never hit Oracle | The 60s scheduler tick was the only path from Oracle into the mirror; Refresh just re-ran the read query against unchanged data | Manual trigger mirrors the read auth (`get_current_user_required`) so any authenticated user (the JSW tab is accessible to all roles) can use it; in-flight guard prevents button-mashing from fanning out into N parallel Oracle round-trips; cache invalidation after sync so `/api/jsw/dashboard` recomputes on the next read |
| 54 | 2026-05-08 18:15 | frontend/src/components/JswTripsTab.jsx:60-104,170-188 | `onClick={() => setRefreshSeq(s => s + 1)}` — Refresh just re-fetched the mirror, no sync trigger; no loading/disabled state | New `syncing` state + `handleRefresh` `useCallback`: `await api.post('/api/jsw/sync-now', {})` then bump `refreshSeq` then clear `syncing`; button is `disabled` while syncing with `cursor: wait`, opacity 0.6, label "Syncing…" | Refresh button gave operators no feedback that anything was happening, and no actual sync was triggered (see #53) | UX gap from #53 | Soft-fails on POST error (network blip / Oracle unreachable) — still bumps `refreshSeq` so the user at least sees the latest mirror state. `console.warn` instead of throwing so the UI never gets stuck. Title attribute updates: "Pull fresh data from JSW WBATNGL" / "Syncing from JSW…" |
| 55 | 2026-05-11 10:50 | test_kotaiah_bf5_fix.py | (did not exist) | NEW 165 LOC ad-hoc diagnostic | Kotaiah replied 11-May "I am able to view data" with screenshot, but we needed to verify the recompile worked from the SMS4 PC's ITROSYSP login (his screenshot was from his DBA session) | Recompiling a view in a DBA session doesn't change what an end-user ITROSYSP can read underneath it — need to test from ITROSYSP specifically | Probes 3 BF5/BF3 views from ITROSYSP, reports row count + ORA error per view. Result: BF5.WB_TRANS_DATA_ITRO still ORA-04063 for ITROSYSP (Kotaiah recompiled in his own session, missed the underlying grant); ZWB workaround still working (17,632 rows). DEP010 stays open per user instruction |
| 56 | 2026-05-11 16:50 | test_hts_connectivity.py | (did not exist) | NEW 230 LOC ad-hoc diagnostic — 4 commits across the day refined it | Vasagerappa confirmed network is fully open between 10.10.23.193 and 10.10.70.227 — but ITROSYSP still got ORA-01034 from HTS. Before pushing the issue to JSW for V$INSTANCE / lsnrctl diagnostics, needed to rule out client-side misconfig | The 06-May test used ITROSYSP for HTS (WBATNGL user, wrong DB) AND just `JVMLPROD` as service (JSW's listener registers only `JVMLPROD.JSW.IN`) — error messages were misleading: ORA-01034 looked like "DB is down" when actually it was "service name not registered for short form" | Probes 7 connection-string variations (FQDN/SID/short, Easy Connect/TNS descriptor). Discovered: BF5.WB_TRANS_DATA_ITRO has no equivalent yet — but HTS itself is reachable via JVMLPROD.JSW.IN AND user must be ICT_IFACE (with underscore) not ITROSYSP. Password ICTIFACE from Hari's 03-Apr email. Final test result: 2/7 variations PASS, HTS.VW_HTS_HOTMETAL_DATA returns 123 rows, 8 columns. DEP007 connectivity CLOSED |
| 57 | 2026-05-11 17:30 | test_all_db_connections.py | (did not exist) | NEW 241 LOC unified pre-flight check | Before sending the public DEP007 closure email, needed one consolidated test that proves all 3 JSW DBs (MySQL SuVeechi + Oracle WBATNGL + Oracle HTS) are currently working — not relying on logs or scattered test scripts | Email going to 20+ JSW + Deevia recipients claiming "HTS works, DEP007 closed" — need one green-light artifact | Tests each DB in sequence with TCP reach + driver connect + smoke query + row count. MySQL/WBATNGL creds read from .env (already configured), HTS password prompted interactively (not yet in .env). Result on SMS4 11-May ~17:35 IST: SuVeechi 53 rows, WBATNGL BF3 216,938 / BF5 17,637, HTS 123 rows. All 3 PASS. Email sent immediately after |
| 58 | 2026-05-11 17:40 | backend/.env.example | (no HTS block) | Added HTS_* block: SYNC_ENABLED, HOST, PORT, USER (ICT_IFACE), PASSWORD, SERVICE (JVMLPROD.JSW.IN — FQDN), VIEW, SYNC_INTERVAL_SECONDS + extensive comment block documenting the two gotchas (FQDN suffix and ICT_IFACE vs ITROSYSP) so the next person doesn't repeat today's investigation | DEP007 connectivity proven (#56-57). When the HTS sync module is eventually written, the env contract needs to be in place | Anything that interacts with HTS needs HTS_* envs distinct from WBATNGL_* (different server, different user, different password, different service-name format). Inline comments call out both gotchas so they aren't forgotten | Mirrors structure of existing SUVEECHI_* and WBATNGL_* blocks; shares ORACLE_INSTANT_CLIENT_DIR with WBATNGL since both use the same thick-mode Instant Client |

## Net Change

### Backend
- **LOC:** 18,402 → 18,127 (−275 LOC, −1.5%)
- **Files deleted:** 1 (`backend/routes/geofence.py`)
- **Files created:** 1 (`backend/utils/analytics_helpers.py`)
- **Files modified:** 8 (main.py, models.py, daily_plans.py, statistics.py, performance_analytics.py, plans.py, CLAUDE.md, trip_crud.py)

### Frontend
- **LOC:** 35,320 → 33,655 (−1,665 LOC, −4.7%)
- **Files deleted:** 4 (AdminPlanning.jsx, LiveOperations.jsx, PlanningHistory.jsx, App.css)

### Combined
- **Total LOC saved:** −1,940 LOC

## Verification

- `python -c "from backend.main import app; print('OK')"` → OK
- No test files reference deleted models
- All 24 routers (was 25) registered correctly
- Tests: 166/166 passing (after #11 fix; was 165/166)
- Commit `0918ef55` pushed to `origin/feature/strip-security-to-local-auth`
- `npm run build` → OK in 8.12s, all chunks generated
