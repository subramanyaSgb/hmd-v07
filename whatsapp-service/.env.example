# WhatsApp Service Configuration
PORT=3002
NODE_ENV=development

# Session storage: 'file' or 'redis'
SESSION_STORE=file

# Redis configuration (if SESSION_STORE=redis)
REDIS_HOST=localhost
REDIS_PORT=6379

# Rate limiting
RATE_LIMIT_PER_MINUTE=20

# Logging
LOG_LEVEL=info
