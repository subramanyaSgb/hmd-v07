# Changes Tracker

| # | Timestamp | File | Previous | New | Issue | Cause | Fix |
|---|-----------|------|----------|-----|-------|-------|-----|
| 1 | 2026-04-23 10:38 | backend/routes/geofence.py | 123 LOC route file with 5 endpoints | DELETED | Dead code — zero frontend calls, zero backend imports | Geofence feature scaffolded for future GPS but never wired in | Removed entire file |
| 2 | 2026-04-23 10:38 | backend/main.py | Imported + registered geofence_router | Lines removed | Route registration of dead module | Geofence file deleted | Removed `from .routes.geofence import router as geofence_router` and `app.include_router(geofence_router)` |
| 3 | 2026-04-23 10:38 | backend/database/models.py | Geofence model class (15 LOC at line 384) | DELETED | Dead model — only referenced by deleted geofence route | Geofence route deleted | Removed Geofence class |
| 4 | 2026-04-23 10:38 | backend/database/models.py | LocationQueue, TripPattern, CostConfig, PerformanceScore, Achievement, AnomalyAlert, SafetyChecklist, SafetyIncident, AuditLog (~120 LOC, lines 428-547) | DELETED | Dead models — zero queries anywhere in backend or frontend | Schema-only stubs never wired into business logic | Removed 9 model classes; tables remain in DB if previously created (no migration to drop) |
| 5 | 2026-04-23 10:38 | backend/utils/analytics_helpers.py | (did not exist) | NEW 18 LOC | Duplicated `get_config()` in 4 files + `get_avg_capacity()` in 2 files | Helpers copy-pasted instead of shared | Created shared module with both helpers |
| 6 | 2026-04-23 10:38 | backend/routes/daily_plans.py | Inline `get_config` (3 LOC) | Imports from analytics_helpers | Duplication | See #5 | Replaced with `from ..utils.analytics_helpers import get_config` |
| 7 | 2026-04-23 10:38 | backend/routes/statistics.py | Inline `get_config` + `get_avg_capacity` (12 LOC) | Imports from analytics_helpers | Duplication | See #5 | Replaced with shared imports |
| 8 | 2026-04-23 10:38 | backend/routes/performance_analytics.py | Inline `get_config` + `get_avg_capacity` (12 LOC) | Imports from analytics_helpers | Duplication | See #5 | Replaced with shared imports |
| 9 | 2026-04-23 10:38 | backend/routes/plans.py | Inline `get_config` (3 LOC) | Imports from analytics_helpers | Duplication | See #5 | Replaced with shared import |
| 10 | 2026-04-23 10:38 | CLAUDE.md | Geofence model + API endpoints documented | Removed | Stale docs reference deleted code | Cascading update | Removed `Geofence` from key models list and `Geofences` API section |
| 11 | 2026-04-23 10:55 | backend/routes/trip_crud.py:20 | `from ..utils.soft_delete import active_only` | `from ..utils.soft_delete import active_only, soft_delete` | `NameError: name 'soft_delete' is not defined` at line 789 broke `test_delete_pending_trip` | Missing symbol in import list — `soft_delete()` called but never imported | Added `soft_delete` to import |
| 12 | 2026-04-23 10:55 | ~/.claude/settings.json:161 | `code-review-graph update --quiet` | `code-review-graph update` | PostToolUse hook errored on every Edit/Write/Bash | `--quiet` flag not supported by `code-review-graph update` subcommand | Removed `--quiet` flag (effective next session) |
| 13 | 2026-04-23 10:50 | git | Working tree had uncommitted changes | Committed `0918ef55` to `feature/strip-security-to-local-auth`, pushed to origin | Need to persist tracked refactor changes | User approved Phase A | Committed only tracked files (main.py + models.py); untracked route edits stay on disk per branch convention |
| 14 | 2026-04-23 11:10 | frontend/src/pages/AdminPlanning.jsx | 511 LOC page component | DELETED | Dead code — zero imports, not in App.jsx routes | Superseded by MonthlyPlanning.jsx but never removed | Removed file |
| 15 | 2026-04-23 11:10 | frontend/src/pages/LiveOperations.jsx | 787 LOC page component | DELETED | Dead code — zero imports, not in App.jsx routes | Operations.jsx serves same purpose; LiveOps was duplicate | Removed file |
| 16 | 2026-04-23 11:10 | frontend/src/pages/PlanningHistory.jsx | 325 LOC page component | DELETED | Dead code — zero imports, not in App.jsx routes | PlanHistory component (in MonthlyPlanning) replaced this | Removed file |
| 17 | 2026-04-23 11:10 | frontend/src/App.css | 42 LOC | DELETED | Dead — zero imports, no JS reference | index.css used instead, App.css orphaned | Removed file |
| 18 | 2026-04-23 11:10 | CLAUDE.md | LiveOperations.jsx documented in pages list | Removed | Stale doc reference | Cascading update | Removed bullet from `frontend/src/pages/` list |
| 19 | 2026-04-23 11:15 | backend/alembic/versions/h1i2j3k4l5m6_drop_dead_tables.py | (did not exist) | NEW migration | 10 orphaned tables in DB after model deletes (#3, #4) | Models removed but DDL tables persisted | Conditional `DROP TABLE` for: geofences, achievements, anomaly_alerts, audit_logs, cost_configs, location_queues, performance_scores, safety_checklists, safety_incidents, trip_patterns. Downgrade no-op |
| 20 | 2026-04-23 11:15 | backend/alembic/versions/h1i2j3k4l5m6_drop_dead_tables.py | down_revision = 'g1h2i3j4k5l6' | down_revision = '3878275160e7' | `Multiple head revisions` error — branched off wrong parent | `drop_sound_alerts_enabled` already chained from `g1h2i3j4k5l6` | Re-parented to `3878275160e7` to merge branches |
| 21 | 2026-04-23 11:15 | DB schema | 10 orphaned tables present | DROPPED | Dead tables waste schema, confuse devs | Migration applied via `alembic upgrade head` | DB head now at `h1i2j3k4l5m6` |
| 22 | 2026-05-06 17:00 | backend/utils/suveechi_sync.py | (did not exist) | NEW 152 LOC | Need live torpedo GPS for live tracking map | SuVeechi MySQL view shared 02-Apr; no integration yet | Pull `vw_unit_status_ist` → upsert FleetLiveLocation + FleetManagement; normalize 'TLC 01' → 'TLC-01'; map status Idle/Moving→Operating, Ign Off→Maintenance; invalidate fleet_cache after each sync |
| 23 | 2026-05-06 17:00 | backend/main.py | scheduler runs WhatsApp daily report only | Added `schedule_suveechi_sync()` | Wire SuVeechi sync to APScheduler interval trigger | Job needed for live map updates | New IntervalTrigger every `SUVEECHI_SYNC_INTERVAL_SECONDS` (default 10s); `asyncio.to_thread` wraps sync pymysql call; daily prune at 02:00 of stale locations >24h |
| 24 | 2026-05-06 17:00 | backend/requirements.txt | no MySQL driver | Added `pymysql>=1.1.0` | SuVeechi sync uses MySQL | New dep | Package added under Caching section |
| 25 | 2026-05-06 17:00 | backend/.env.example | no SuVeechi config | Added 7 vars | Document SuVeechi connection settings | Required for sync feature | `SUVEECHI_SYNC_ENABLED`, `SUVEECHI_HOST/PORT/USER/PASSWORD/DB/VIEW`, `SUVEECHI_SYNC_INTERVAL_SECONDS` |
| 26 | 2026-05-07 09:00 | backend/routes/fleet.py | `GET /api/fleet/live` returned only x/y/fleet_id/type | Extended response with `status`, `capacity`, ISO `last_updated` via LEFT JOIN FleetManagement | Frontend needs status to colour markers + show drawer header | Marker colours and drawer status badge depend on this | Added outer-join + ISO-format datetime; cache key/TTL unchanged |
| 27 | 2026-05-07 09:00 | frontend/src/utils/torpedoStatus.js | (did not exist) | NEW 32 LOC | Single source of truth for status→colour and status→short-label maps | Avoid duplicating mapping in marker, drawer, legend | Handles HMD vocabulary (Operating/Assigned/Maintenance) AND SuVeechi vocabulary (Idle/Moving/Ign Off); default slate colour for unknown |
| 28 | 2026-05-07 09:00 | frontend/src/pages/Dashboard.jsx | `createFleetIcon(id)` used hard-coded `hsl(var(--primary))` for all torpedoes; label was just `{id}` | `createFleetIcon(id, status)` — colour from `statusColor()`, label `{id} · {short}` | Operators couldn't distinguish Idle/Moving/Ign Off torpedoes at a glance | Status visibility | Marker box widened to 60px; fleet marker call site passes `fleet.status` |
| 29 | 2026-05-07 09:00 | frontend/src/pages/Dashboard.jsx | Map opened at default centre, user panned/zoomed manually | NEW `FitBoundsOnFleet` helper component + `hasFittedFleet` state | Auto-zoom to all 53 torpedoes on first load | UX improvement | Fits once on first non-empty fleet load with `padding: [60,60], maxZoom: 16`; idempotent via `hasFitted` flag |
| 30 | 2026-05-07 10:55 | frontend/src/components/TorpedoDrawer.jsx | (did not exist) | NEW ~470 LOC | Phase 2 — clicking a torpedo only showed a tiny `"TORPEDO: TLC-16 LIVE TRACKING ACTIVE"` popup, no useful info | Operators needed live position, current/last trip, history, capacity at a glance | 420px right-side slide-in drawer with sticky header (fleet_id + status badge + "updated Ns ago" + ✕) and 5 sections: Live Position (5s poll, derived speed via Haversine), Current Trip (10s poll only when Assigned), Last Trip, Trip History (20 rows, manual refresh), Maintenance & Capacity. Close on ✕/backdrop/Esc. Reuses existing `/api/fleet/live` and `/api/fleet-management/{fleet_id}/details` — no backend change |
| 31 | 2026-05-07 10:55 | frontend/src/pages/Dashboard.jsx | Torpedo `<Marker>` rendered a `<Popup>` with stub text; no drawer state | Added `selectedFleetId` state, replaced popup with `eventHandlers={{ click: () => setSelectedFleetId(fleet.fleet_id) }}`, rendered `<TorpedoDrawer fleetId={selectedFleetId} onClose={...} />` at page level | Wire marker click → drawer | Phase 2 entry point | Imported `TorpedoDrawer`; removed Popup JSX from torpedo marker; drawer mounted as sibling of map container so it overlays full viewport |
| 32 | 2026-05-07 11:05 | frontend/src/components/TorpedoDrawer.jsx | Backdrop `zIndex: 1100`, drawer `zIndex: 1101` | Backdrop `2200`, drawer `2201` | On SMS4 the drawer's sticky header (fleet_id, status badge, ✕) was hidden behind the app's top bar — only the colored top-border stripe peeked through | App header is `z-index: 2000` and sidebar `z-index: 2100` (in `index.css`); my 1101 was below both | Bumped backdrop+drawer z-indices above sidebar so the whole drawer overlays page chrome (standard side-drawer pattern). Verified on SMS4 with screenshot showing `TLC-05 · IDLE · updated 0s ago` header rendering correctly |
| 33 | 2026-05-07 14:08 | frontend/src/pages/Dashboard.jsx | `currentTime` state + `setInterval(setCurrentTime, 1000)`; `planningSummary` state + `/api/daily-plans/dashboard-summary` fetched on mount AND every 5s poll; `error` state + `setError` calls | All three removed. Fetches reduced to `/api/locations` + `/api/weighbridges` (initial) and `/api/fleet/live` only (poll) | **PERF REGRESSION**: SMS4 page showed "This page isn't responding" once SuVeechi started feeding 53 live torpedoes | `currentTime` was set every 1s but never read in JSX — caused 1 Hz Dashboard re-render. Each render rebuilt all 53 torpedo markers (`<Marker icon={createFleetIcon(...)} position={[x,y]}>` produces new icon + new position every render → react-leaflet calls `marker.setIcon()` and `setLatLng()` which replace marker DOM HTML). 53 markers × DOM rebuild × 1 Hz ≈ 100-160 ms main-thread work every second, blocking event loop. Pre-SuVeechi the same code ran but FleetLiveLocation was empty so the loop was free. ESLint had already flagged `currentTime`, `planningSummary`, `error` as unused | Removed all three pieces of dead state + their setters + the 1s interval + the dead `/api/daily-plans/dashboard-summary` poll round-trip. Stops the 1 Hz re-render storm at the source |
| 34 | 2026-05-07 14:08 | frontend/src/pages/Dashboard.jsx | Inline torpedo `<Marker key={...} position={[fleet.x, fleet.y]} icon={createFleetIcon(...)} eventHandlers={{click: () => setSelectedFleetId(...)}} />` — every prop allocated fresh per render | New `FleetMarker = memo(({fleet,onClick}) => {...}, customEquality)` subcomponent with `useMemo` for `icon`, `position`, `eventHandlers`, `useCallback` for `handleClick`. Custom `arePropsEqual` compares `fleet_id`/`x`/`y`/`status`/`onClick` | Defense-in-depth — even when re-renders DO fire (every 5s poll), unchanged torpedoes should skip DOM work | react-leaflet's `<Marker>` triggers `setIcon`/`setLatLng` on prop reference change, even if values identical | `React.memo` skips re-render when no torpedo data changed; internal `useMemo` keeps icon+position references stable across renders. Idle torpedoes (most of the fleet) now incur **zero** DOM work per poll. Combined with `useCallback` on `handleFleetClick`/`handleDrawerClose` in Dashboard so child memos hold |
| 35 | 2026-05-07 14:08 | frontend/src/pages/Dashboard.jsx | `createFleetIcon(id, status)` allocated a fresh `L.divIcon` on every call | Module-level `_fleetIconCache` Map keyed by `${id}|${status}` returns the same divIcon instance for repeat (id, status) pairs | Belt-and-braces — guarantees icon-prop reference stability even outside the `useMemo` path | Function-level memoization | Bounded ~265 entries (53 fleets × ~5 statuses); GC-safe |
| 36 | 2026-05-07 14:08 | frontend/src/pages/Dashboard.jsx | `<TorpedoDrawer onClose={() => setSelectedFleetId(null)} />` — new function reference every render | `const handleDrawerClose = useCallback(() => setSelectedFleetId(null), [])` then `onClose={handleDrawerClose}` | Drawer's Esc-key effect listed `onClose` in deps → tore down + re-added the global keydown listener on every Dashboard render while drawer was open | New closure each render | Stable callback identity prevents listener thrash |
| 37 | 2026-05-07 14:08 | frontend/src/components/TorpedoDrawer.jsx | `tick >= 0 && formatMinutes(elapsedMinutes(currentTrip.assigned_at))` — odd guard hack | `formatMinutes(elapsedMinutes(currentTrip.assigned_at))` (plain) | Code clarity — `tick` triggers re-render at the component level, the `&&` was a no-op | Confused refactor leftover | Removed the guard; behaviour identical |
| 38 | 2026-05-07 14:30 | backend/routes/fleet.py | `/api/fleet/live` subquery used `MAX(last_updated) GROUP BY fleet_id`, then JOINed on `(fleet_id == fleet_id) AND (last_updated == max_updated)` | Subquery now uses `MAX(id) GROUP BY fleet_id`, JOIN on `id == max_id` | **CRITICAL: API was returning ~28,000 rows** (53 torpedoes × hundreds of dupes each) — frontend rendered 28k markers stacked, page locked up | SuVeechi reports the same `reporttime_ist` for an idle torpedo across many sync ticks; sync appended a new FleetLiveLocation row each tick with that same timestamp; the JOIN-on-last_updated then matched ALL of them per fleet_id | `id` is unique + monotonic, so `MAX(id) GROUP BY fleet_id` returns exactly one row per torpedo regardless of timestamp duplicates |
| 39 | 2026-05-07 14:30 | backend/utils/suveechi_sync.py | `db.add(FleetLiveLocation(...))` always — appended every tick, no dedupe | Skip insert when `MAX(last_updated)` for this fleet_id is already `>= reported`; only insert when SuVeechi reports a strictly newer timestamp | Stops the dupe accumulation at the source so the table no longer bloats by 53 rows per 10s for idle torpedoes | Append-only design assumed every tick brought a new timestamp, but SuVeechi's `reporttime_ist` doesn't tick when a unit is idle | Added one `MAX(last_updated)` lookup per fleet_id per tick; cheap with the index that should exist on `fleet_id`. Imported `from sqlalchemy import func` |
| 40 | 2026-05-07 14:30 | backend/utils/cleanup_fleet_location_dupes.py | (did not exist) | NEW one-time cleanup script | After fix #38 the existing ~28k dupes are ignored by the query but still consume disk + slow `MAX(id)` scans | Existing data leakage from the bug | Keeps only the row with `MAX(id)` per `fleet_id`; deletes the rest. Run once via `python -m backend.utils.cleanup_fleet_location_dupes` on SMS4 |
| 41 | 2026-05-07 14:30 | frontend/src/pages/Dashboard.jsx | `pollLiveUpdates` set `setFleetLocations(arr)` directly | Defensive Map-based dedupe by `fleet_id`, keeping the row with newest `last_updated` (highest `id` as tiebreaker), then `setFleetLocations(Array.from(byId.values()))` | Belt-and-braces — never render two markers for the same torpedo even if backend ever leaks a duplicate again | Defense in depth | O(n) once-per-poll dedupe at the client edge |
| 42 | 2026-05-07 14:50 | frontend/src/pages/Settings.jsx | Map Configuration card had Default Zoom + Base Map Style only | Added "Torpedo Status Legend" toggle row with `settings-toggle` switch; persists to `localStorage['hmd_show_torpedo_legend']` (default `true`); dispatches `hmd:settings-changed` window event so Dashboard updates live without refresh | User asked for a toggleable on-map count summary | New feature | New `showTorpedoLegend` state mirroring localStorage; toggle handler writes localStorage + fires the custom event |
| 43 | 2026-05-07 14:50 | frontend/src/pages/Dashboard.jsx | No fleet-status summary on the map | New top-right overlay box (when toggle is ON) showing `Total · Idle · Moving · Trip · Maint` counts with status colors from `torpedoStatus.js` | User wanted at-a-glance count + status breakdown | Same | `torpedoBuckets` `useMemo` over `fleetLocations` maps SuVeechi (Idle/Moving/Ign Off) and HMD (Operating/Assigned/Maintenance) vocab into 4 buckets + total; reusable `legendChip(label, value, color)` helper. Dashboard listens to `hmd:settings-changed` so toggling in Settings updates the map immediately |
| 44 | 2026-05-07 15:30 | frontend/src/pages/Dashboard.jsx | Legend chip layout was at top-right — overlapped the producer/consumer/weighbridge legend (truncating "PRODUCER" on narrower viewports) | Moved to bottom-left of the map (`bottom: 24px; left: 24px`) | Layout collision | Side-by-side overlay boxes don't fit on a regular SMS4 viewport | Cleaner separation: top-left = page title, top-right = node legend, bottom-left = torpedo counts |
| 45 | 2026-05-07 15:45 | backend/utils/wbatngl_capacity_sync.py | (did not exist) | NEW ~210 LOC | **Phase 3** — `FleetManagement.capacity` was hard-coded to 360 MT for every torpedo; real capacities vary (BF3/BF4 ~350-375, BF5 ~300-320 per WBATNGL `NET_WEIGHT` history) | Default seed value | Oracle thick-mode connection (re-using pattern from `db_test_v2.py`), idempotent `_ensure_thick_mode` guard so APScheduler can run it repeatedly, scans `BF3."WB_TRANS_DATA_ITRO"` + `BF5."ZWB_TRANSACTION_DATA_ITRO_B"` with `MAX(NET_WEIGHT) GROUP BY LADLENO`, normalises LADLENO ("TLC 01" / "TLC01" / "TLC-1" → canonical "TLC-NN"), `derive_capacity = clamp(observed × 1.05, [300, 400])` MT, only `UPDATE` if value moved by ≥0.05 MT, per-torpedo log line. `run_once()` is idempotent and safe to call manually |
| 46 | 2026-05-07 15:45 | backend/main.py | Scheduler had only WhatsApp daily report + SuVeechi sync | Added `schedule_wbatngl_capacity_sync()` invoked from `startup_event`. CronTrigger at 03:00 IST daily (after the 02:00 SuVeechi prune); guarded by `WBATNGL_SYNC_ENABLED=true`; runs in `asyncio.to_thread` so the Oracle round-trip doesn't block the event loop | Wire the new sync to APScheduler | Phase 3 schedule | `replace_existing=True, max_instances=1, coalesce=True` matches SuVeechi job conventions |
| 47 | 2026-05-07 15:45 | backend/routes/fleet.py | (no manual trigger for capacity sync) | NEW `POST /api/fleet-management/sync-capacities` (admin/trs only). Runs `wbatngl_run_once()` off-thread, invalidates `fleet_management` + `live_fleet_locations` caches, logs an audit event | Operator/admin shouldn't have to wait until 03:00 to see the backfill take effect | Manual override | `Depends(get_db)` so the audit log entry uses a properly-scoped session; `Depends(require_roles("admin", "trs"))` gates access |
| 48 | 2026-05-07 15:45 | backend/.env.example | (no WBATNGL block) | Added 7 vars: `WBATNGL_SYNC_ENABLED`, `WBATNGL_HOST/PORT/USER/PASSWORD/SERVICE`, `ORACLE_INSTANT_CLIENT_DIR` | Document Oracle connection settings | Required for the new sync | Mirrors structure of the existing SUVEECHI block |
| 49 | 2026-05-07 15:45 | backend/requirements.txt | only `pymysql` for SuVeechi | Added `oracledb>=2.0.0` | New Oracle driver dep | Phase 3 needs Oracle | Thick mode requires Instant Client at the path in `ORACLE_INSTANT_CLIENT_DIR` |
| 50 | 2026-05-07 16:30 | backend/utils/wbatngl_capacity_sync.py | `HEADROOM_PCT=1.05`, `CAP_MAX_MT=400.0`, no SQL filter on NET_WEIGHT | `HEADROOM_PCT=1.0` (no headroom — observed MAX is the capacity, no need to inflate), `CAP_MAX_MT=600.0`, new `SANE_NW_MIN=100.0` + `SANE_NW_MAX=1000.0` constants applied as SQL `WHERE NET_WEIGHT BETWEEN :nw_min AND :nw_max` filter | First Phase 3 run on SMS4 clamped EVERY torpedo to 400 MT — typical observed MAX was 425–485 MT (well above the 400 cap), and one row in `BF3.WB_TRANS_DATA_ITRO` had a 6747 MT junk value (TLC-19) that would have skewed that fleet | Plan's clamp [300, 400] was modeled on a 350-380 MT expected range that didn't match real WBATNGL data (JSW Vijayanagar torpedoes routinely carry ~450 MT NET); the ×1.05 headroom on top of MAX further pushed every result past the cap. SQL filter now drops blatant outliers at source so MAX is robust | Bumped cap to 600 MT (admits heaviest legitimate torpedoes), dropped the headroom multiplier (capacity = observed MAX rounded), and added SQL filter to reject NET_WEIGHT < 100 or > 1000 before MAX. Re-running the sync now should produce varied per-torpedo capacities (e.g. TLC-23: 425 MT, TLC-04: 479 MT, TLC-33: 483 MT) instead of all clamped to 400 |
| 51 | 2026-05-07 17:00 | backend/utils/wbatngl_capacity_sync.py | `SANE_NW_MAX=1000.0` | `SANE_NW_MAX=550.0` | Second SMS4 run showed TLC-43 at observed 665.6 MT (clamped to 600), ~180 MT above the rest of the fleet; user judged it suspicious data, not a heavier-duty torpedo | The 1000 MT upper bound was loose — caught the obvious 6747 MT junk row, but admitted the borderline 665.6 MT row that's 35% above the next-highest legitimate value | Tightened SANE_NW_MAX to 550 MT — gives ~67 MT margin over TLC-33's legitimate fleet maximum (483.4 MT), drops the 665.6 row at SQL level so MAX falls back to TLC-43's next-highest legitimate cast |
| 52 | 2026-05-08 18:15 | backend/utils/wbatngl_trip_sync.py:238-264 | Conflict-update set: `{col: stmt.excluded[col] for col in update_cols}` where `update_cols` excluded `("id", "trip_id", "synced_at")` → `synced_at` only set on INSERT, never on UPDATE | Same `update_cols` exclusion list, but the conflict-update dict now explicitly stamps `set_dict["synced_at"] = func.now()` on every upsert | UI showed `last sync 3:28:06 AM` frozen for hours despite the 60s WBATNGL tick logging `fetched=2 upserted=2 errors=0` every minute — operators thought the sync was broken | `MAX(synced_at)` (read by `_last_sync_at()` in `routes/jsw.py:60`) only advanced when a brand-new `trip_id` was INSERTED. During quiet periods JSW revises existing trips (weight/temp updates) without creating new TRIP_IDs → no insert → `synced_at` for those rows never moves → label looks stuck even though sync is healthy | Bumped `synced_at` to `func.now()` inside the `set_dict` so every upsert (insert OR update) re-stamps it. `func.now()` compiles to `NOW()` on PG and `CURRENT_TIMESTAMP` on SQLite so existing 62 sync/jsw tests still pass |
| 53 | 2026-05-08 18:15 | backend/routes/jsw.py:298-356 | (no manual sync trigger) | NEW `POST /api/jsw/sync-now` endpoint with module-level `_sync_in_flight` guard, runs `wbatngl_trip_sync.run_once()` via `asyncio.to_thread`, invalidates `CACHE_KEY_DASHBOARD` after success | The JSW tab's Refresh button only re-read the local mirror — operators expected it to "go fetch from JSW now" but it never hit Oracle | The 60s scheduler tick was the only path from Oracle into the mirror; Refresh just re-ran the read query against unchanged data | Manual trigger mirrors the read auth (`get_current_user_required`) so any authenticated user (the JSW tab is accessible to all roles) can use it; in-flight guard prevents button-mashing from fanning out into N parallel Oracle round-trips; cache invalidation after sync so `/api/jsw/dashboard` recomputes on the next read |
| 54 | 2026-05-08 18:15 | frontend/src/components/JswTripsTab.jsx:60-104,170-188 | `onClick={() => setRefreshSeq(s => s + 1)}` — Refresh just re-fetched the mirror, no sync trigger; no loading/disabled state | New `syncing` state + `handleRefresh` `useCallback`: `await api.post('/api/jsw/sync-now', {})` then bump `refreshSeq` then clear `syncing`; button is `disabled` while syncing with `cursor: wait`, opacity 0.6, label "Syncing…" | Refresh button gave operators no feedback that anything was happening, and no actual sync was triggered (see #53) | UX gap from #53 | Soft-fails on POST error (network blip / Oracle unreachable) — still bumps `refreshSeq` so the user at least sees the latest mirror state. `console.warn` instead of throwing so the UI never gets stuck. Title attribute updates: "Pull fresh data from JSW WBATNGL" / "Syncing from JSW…" |
| 55 | 2026-05-11 10:50 | test_kotaiah_bf5_fix.py | (did not exist) | NEW 165 LOC ad-hoc diagnostic | Kotaiah replied 11-May "I am able to view data" with screenshot, but we needed to verify the recompile worked from the SMS4 PC's ITROSYSP login (his screenshot was from his DBA session) | Recompiling a view in a DBA session doesn't change what an end-user ITROSYSP can read underneath it — need to test from ITROSYSP specifically | Probes 3 BF5/BF3 views from ITROSYSP, reports row count + ORA error per view. Result: BF5.WB_TRANS_DATA_ITRO still ORA-04063 for ITROSYSP (Kotaiah recompiled in his own session, missed the underlying grant); ZWB workaround still working (17,632 rows). DEP010 stays open per user instruction |
| 56 | 2026-05-11 16:50 | test_hts_connectivity.py | (did not exist) | NEW 230 LOC ad-hoc diagnostic — 4 commits across the day refined it | Vasagerappa confirmed network is fully open between 10.10.23.193 and 10.10.70.227 — but ITROSYSP still got ORA-01034 from HTS. Before pushing the issue to JSW for V$INSTANCE / lsnrctl diagnostics, needed to rule out client-side misconfig | The 06-May test used ITROSYSP for HTS (WBATNGL user, wrong DB) AND just `JVMLPROD` as service (JSW's listener registers only `JVMLPROD.JSW.IN`) — error messages were misleading: ORA-01034 looked like "DB is down" when actually it was "service name not registered for short form" | Probes 7 connection-string variations (FQDN/SID/short, Easy Connect/TNS descriptor). Discovered: BF5.WB_TRANS_DATA_ITRO has no equivalent yet — but HTS itself is reachable via JVMLPROD.JSW.IN AND user must be ICT_IFACE (with underscore) not ITROSYSP. Password ICTIFACE from Hari's 03-Apr email. Final test result: 2/7 variations PASS, HTS.VW_HTS_HOTMETAL_DATA returns 123 rows, 8 columns. DEP007 connectivity CLOSED |
| 57 | 2026-05-11 17:30 | test_all_db_connections.py | (did not exist) | NEW 241 LOC unified pre-flight check | Before sending the public DEP007 closure email, needed one consolidated test that proves all 3 JSW DBs (MySQL SuVeechi + Oracle WBATNGL + Oracle HTS) are currently working — not relying on logs or scattered test scripts | Email going to 20+ JSW + Deevia recipients claiming "HTS works, DEP007 closed" — need one green-light artifact | Tests each DB in sequence with TCP reach + driver connect + smoke query + row count. MySQL/WBATNGL creds read from .env (already configured), HTS password prompted interactively (not yet in .env). Result on SMS4 11-May ~17:35 IST: SuVeechi 53 rows, WBATNGL BF3 216,938 / BF5 17,637, HTS 123 rows. All 3 PASS. Email sent immediately after |
| 58 | 2026-05-11 17:40 | backend/.env.example | (no HTS block) | Added HTS_* block: SYNC_ENABLED, HOST, PORT, USER (ICT_IFACE), PASSWORD, SERVICE (JVMLPROD.JSW.IN — FQDN), VIEW, SYNC_INTERVAL_SECONDS + extensive comment block documenting the two gotchas (FQDN suffix and ICT_IFACE vs ITROSYSP) so the next person doesn't repeat today's investigation | DEP007 connectivity proven (#56-57). When the HTS sync module is eventually written, the env contract needs to be in place | Anything that interacts with HTS needs HTS_* envs distinct from WBATNGL_* (different server, different user, different password, different service-name format). Inline comments call out both gotchas so they aren't forgotten | Mirrors structure of existing SUVEECHI_* and WBATNGL_* blocks; shares ORACLE_INSTANT_CLIENT_DIR with WBATNGL since both use the same thick-mode Instant Client |
| 59 | 2026-05-11 22:00 | docs/plans/2026-05-11-operations-live-design.md + docs/plans/2026-05-11-operations-live-implementation.md | (did not exist) | NEW 438-line design + 1068-line implementation plan for "Operations Live" + "Trip History (Live)" sprint 3 | Need a written, validated design before writing code; need a step-by-step task plan for execution | Per superpowers:brainstorming, two-stage design then plan. 5-section design covers overview/sidebar nav, Page 1 layout, Page 2 layout, data layer (new hts_heat_mirror table + v_trip_heat_story view + 3 API endpoints), 5-phase implementation strategy | Phase 1 detailed into 20 bite-sized TDD tasks; Phases 2-5 outlined as checkpoints, expanded later (avoids stale megaplan) |
| 60 | 2026-05-12 ... | backend/alembic/versions/84b54339b4f5_add_hts_heat_mirror_table.py + bf02ec626f86_add_v_trip_heat_story_view.py + backend/database/models.py (HtsHeatMirror ORM class + Numeric import) | (did not exist) | Sprint 3 Phase 1 Batch A: alembic migration creating hts_heat_mirror table (12 cols + 3 indexes; heat_no UNIQUE NOT NULL is the natural PK; (torpedo_no, torpedo_in_time) composite index for the JOIN window); separate migration creating v_trip_heat_story view (LEFT JOIN wbatngl_trip_mirror ↔ hts_heat_mirror on torpedo_no + closetime ±15/+90 min window so unmatched trips still appear); HtsHeatMirror ORM class mirroring the table schema with docstring noting the 11-May inventory that confirmed HEAT_NO is unique (123/123 distinct) | Need PG mirror of HTS view + 3-DB join structure before the sync module that populates it | Need backend support for Pages 1 + 2 of the sprint | View uses Postgres-specific INTERVAL syntax — tests bypass alembic (use Base.metadata.create_all in SQLite) so no test breakage; production runs `alembic upgrade head` per existing handover convention |
| 61 | 2026-05-12 ... | backend/utils/hts_sync.py + backend/tests/test_hts_sync.py | (did not exist) | Sprint 3 Phase 1 Batch B: HTS sync module mirroring wbatngl_trip_sync.py pattern. 6 functions: normalize_torpedo_no, row_to_mirror_dict, upsert_rows (with synced_at-on-update from #52), watermark_for_view, pull_and_upsert, run_once + _connect_oracle + _ensure_thick_mode with `_ORACLE_THICK_INITIALIZED` idempotency flag. 23 tests (9 normalize + 4 row_to_mirror_dict + 4 upsert + 2 watermark + 2 pull_and_upsert + 1 missing-password + 1 run_once-error-handling). run_once wraps pull/upsert in try/except → db.rollback() + return {"error": str(e)} for parity with WBATNGL. Cursor close in finally so it doesn't leak on error path | Need a way to pull HTS data on a schedule into the local mirror | TDD discipline: write failing test → see fail → minimal impl → see green → commit. 8 commits across the batch (5 TDD pairs + 3 robustness fixes/tests from code review) | Reviewer found 2 important issues vs WBATNGL precedent (missing thick-mode idempotency flag, missing run_once error wrap); both fixed before merge. 268 total backend tests pass (was 245 baseline; 23 new) |
| 62 | 2026-05-12 ... | backend/main.py | (no HTS scheduler) | Added schedule_hts_sync() function and invocation in startup_event. Same pattern as schedule_wbatngl_trip_sync(): no-arg signature, references module-level scheduler, env-gated by HTS_SYNC_ENABLED=true, default 300s interval, runs run_once via asyncio.to_thread, replace_existing=True/max_instances=1/coalesce=True | Need the HTS sync to fire on a schedule | Mirrors wbatngl trip sync scheduling exactly | Plan stub had `(scheduler)` arg; implementer correctly adjusted to no-arg to match the actual precedent. Spec reviewer approved deviation |
| 63 | 2026-05-11 17:45 | backend/alembic/versions/c3e8d219a4b1_add_fleet_live_locations_fleet_id_index.py | (no index on (fleet_id, last_updated)) | NEW composite index `ix_fleet_live_locations_fleet_id_last_updated` on `fleet_live_locations(fleet_id, last_updated DESC)` | SMS4 Phase 1 deploy surfaced a slow query: `SELECT MAX(last_updated) FROM fleet_live_locations WHERE fleet_id = ?` at 170.8 ms under concurrent-sync contention (3 syncs fired at the same 60s minute boundary: SuVeechi 10s + WBATNGL 60s + new HTS 60s). The MAX-per-fleet_id query runs 53× per SuVeechi tick (every 10s) as the dedup guard from changes_tracker #39 | No composite (fleet_id, last_updated) index → PG falls back to sequential scan over the whole table for each torpedo, every tick | Created index via raw SQL execute (so DESC ordering can be expressed). Index is idempotent (IF NOT EXISTS). Took ~50 ms to apply on SMS4. Slow query disappeared after deploy. Index also benefits the FleetLiveLocation API which does similar MAX-by-fleet_id lookups. Not blocking Phase 1; spotted during deploy monitoring |
| 64 | 2026-05-12 12:00 | backend/routes/operations.py + backend/main.py | (did not exist) | NEW module: 594 LOC route file + 3 endpoints registered in main.py after jsw.router. | Phase 2 of operations-live sprint — backend API layer for the new Page 1 (Operations Live cockpit) and Page 2 (Trip History Live) pages | Per implementation plan 2026-05-12-operations-live-phase-2.md | Scaffolding constants (MATCH_WINDOW_BEFORE=15min, MATCH_WINDOW_AFTER=90min, WEIGHT_ANOMALY_THRESHOLD=10%, CACHE_KEY_DASHBOARD, CACHE_KEY_TRIP_DETAIL, etc.) + 3 pure helpers (`_time_window_to_cutoff`, `find_matched_heats`, `compute_anomaly_flags`) + 4 endpoint-private helpers (`_last_sync_at`, `_build_empty_converter_card`, `_row_to_dict`, `_matched_converters` pop pattern) + 3 endpoints: `GET /api/operations-live/dashboard` (5s cache, returns kpi_strip + converters[6] + active_trips + activity_feed + last_sync_at), `GET /api/trip-history-live` (paginated, filtered by date range / status / converter / search), `GET /api/trip-history-live/:trip_id` (10s cache, returns trip detail + matched heats + position). All behind `get_current_user_required`. Cross-dialect rule honoured — Python `timedelta` arithmetic only, no PG-only INTERVAL SQL in endpoint code. Match window: `closetime − 15 min .. closetime + 90 min`. Weight anomaly threshold: 10%. Active-trips cap: 200 candidate rows (steady-state <20 in production) |
| 65 | 2026-05-12 12:00 | backend/tests/test_operations_endpoints.py | (did not exist) | NEW 684 LOC test file: 60 tests across 11 classes covering all 3 helpers + 3 endpoints | Coverage for Phase 2 (helpers, endpoint shape, KPI computation, converter state machine, active-trips reuse, activity feed, cache TTL, list filter scaffolding, list enrichment, list filter exhaustive coverage, detail skeleton, detail content) | TDD-driven from the implementation plan; ~30+ tests run-once + commit per task across Batches A-D | Uses conftest's `db_session`/`auth_headers` fixtures + new `trip_at`/`heat_at` row-factory fixtures local to this file (with `closetime=None` short-circuit for empty-trip case). Cross-dialect (no PG-only SQL); SQLite-compatible match-window via Python `timedelta` arithmetic. All 60 tests green at HEAD (commit de2491f) |
| 66 | 2026-05-12 12:00 | (no file change) | n/a | Baseline test count went from 268 → 328 (+60 new tests in this phase). Full backend suite green on SMS4-equivalent SQLite at HEAD `de2491f`. App route count: 162. Two test_trip_validation.py tests are occasionally flaky due to test-collection ordering; pre-existing, pass in isolation, unrelated to this sprint | Phase 2 completion checkpoint | n/a | n/a |
| 67 | 2026-05-12 11:00 | backend/routes/operations.py + backend/tests/test_operations_endpoints.py | (Phase 2 used datetime.utcnow() everywhere) | NEW `_now_ist_naive()` helper + `_IST_OFFSET = timedelta(hours=5, minutes=30)` constant; 5 callsites swapped from `utcnow()` to `_now_ist_naive()` (today_cutoff, feed_horizon, converter elapsed, active_trips elapsed, `_time_window_to_cutoff`) | SMS4 verification 2026-05-12 showed `active_trips[].elapsed_minutes` returned -290 min for IST-recent trips; `activity_feed` empty because `feed_horizon` was offset by 5:30 h; `production_today` undercounted between IST midnight and 05:30 IST | WBATNGL/HTS source Oracle tables store timestamps as IST-naive wall-clock (no tzinfo); Phase 2 code used `datetime.utcnow()` as the "now" anchor, off by the IST offset | Helper docstring documents the convention: we shift our internal "now" anchor rather than converting source timestamps at sync time (which would break operator-visible parity with WBATNGL clocks). `TestTimeWindow` assertions and IST-relative seeders updated accordingly. Commit `5d106a8` |
| 68 | 2026-05-12 11:00 | backend/routes/operations.py + backend/tests/test_operations_endpoints.py | `idle_torpedoes` filtered `FleetLiveLocation.type == "Idle"`; `active_trips[].current_status` read `FleetLiveLocation.type` | `idle_torpedoes` now counts `FleetManagement` WHERE `status="Operating"` AND `deleted_at IS NULL` AND `type="torpedo"` (mirrors `active_only()` pattern from routes/fleet.py); `active_trips` uses a batched `FleetManagement.fleet_id IN (...)` lookup; `trip-history-live/:trip_id` `current_torpedo_position` gains a `status` key from `FleetManagement.status` | SMS4 verification: every `active_trips[]` row showed `current_status="torpedo"`; `idle_torpedoes` always 0 even with 53 torpedoes in fleet | `FleetLiveLocation.type` is HARDCODED to `"torpedo"` by `suveechi_sync.py:209` (entity type, NOT operational state). The SuVeechi sync writes mapped operational state to `FleetManagement.status` via `SUVEECHI_STATUS_MAP` (Idle→Operating, Moving→Moving, Ign Off→Maintenance) | Two old tests deleted (`test_active_trips_current_status_from_fleet_live` + `test_idle_torpedoes_uses_latest_per_fleet` — both asserted the wrong contract). Three new tests added covering the FM-based path (idle KPI seeding, current_status from FM, null current_status when no FM row). Commit `526406d` |
| 69 | 2026-05-12 11:00 | backend/routes/operations.py + backend/tests/test_operations_endpoints.py | `candidate_trips = .limit(200)`; KPI `active_trips_now` could report 200 even with HTS frozen | Replaced `.limit(200)` with `out_date >= _now_ist_naive() - timedelta(hours=ACTIVE_TRIP_WINDOW_HOURS)` (ACTIVE_TRIP_WINDOW_HOURS = 6, module-level constant); response payload `[:50]` slice preserved as UI cap | SMS4 verification: `active_trips_now` reported 200 (the candidate cap) because HTS being frozen means EVERY WBATNGL trip is unmatched; KPI was useless | Without HTS data, `find_matched_heats` returns `[]` for every trip; the unbounded candidate query naturally returns all 3000+ history rows, hits the `.limit(200)`, and reports 200 | 6 h window matches operational torpedo cycle (departure → loading → travel → pour typically <3 h); module-level `ACTIVE_TRIP_WINDOW_HOURS` constant for easy retuning. New test asserts trips older than 6 h are excluded. Commit `ba94dff` |
| 70 | 2026-05-12 11:00 | backend/routes/operations.py + backend/tests/test_operations_endpoints.py | `activity_feed` `trip_completed` summary used Unicode "→" (U+2192) | Replaced "→" with ASCII "->" in the f-string | SMS4 verification: cmd rendered the arrow as "ΓåÆ" mojibake under default code pages (CP437/CP850) | Unicode arrow doesn't survive Windows cmd's CP437/CP850; the activity feed is plain text intended for direct display in the operator console | Comment-level Unicode arrows in source code remain (only the user-facing emitted string changed); `heat_started` summary uses `@` (already ASCII), so unchanged. New test asserts ASCII `->` present and Unicode `→` absent in the trip_completed summary. Commit `5190e30` |
| 71 | 2026-05-12 11:00 | backend/routes/operations.py + backend/tests/test_operations_endpoints.py | `current_torpedo_position.status` (detail endpoint) vs `active_trips[].current_status` (dashboard) — same semantic data, different key | Renamed detail endpoint's key to `current_torpedo_position.current_status` for cross-endpoint consistency; new code path FM-lookup (added in #68) was previously untested — added 2 tests | Code-review I1 (naming asymmetry would force frontend alias logic) + I2 (the FM-lookup branch had zero test coverage despite being load-bearing) | Detail endpoint `test_current_position_when_present` seeded only `FleetLiveLocation` so FM lookup was never reached; `test_current_position_null_when_absent` exited at the outer guard | Two tests added under `TestTripDetailContent`: `test_current_position_includes_current_status_from_fm` (seeds both, asserts `FM.status` surfaces) and `test_current_position_current_status_none_when_no_fm_row` (seeds only FleetLiveLocation, asserts graceful None fallback). Commit `a909e38` |
| 72 | 2026-05-12 12:00 | backend/routes/operations.py + backend/tests/test_operations_endpoints.py | _build_empty_converter_card defined but never called; _row_to_dict leaked synced_at/id/source_table; converter sms-null path untested | Deleted _build_empty_converter_card; added _PRIVATE_COLUMNS constant + filter to _row_to_dict (operations.py only — not jsw.py); added test_converter_card_sms_null_when_no_heat_row_has_sms | Final code-review carry-overs from Phase 2 (non-blocking polish items) | Plan-prescribed skeleton helper became dead after the real converter loop landed; row-dict serialization returned every column including sync plumbing; converter sms fallback was tested implicitly via test fixtures that default sms='SMS3' | jsw.py's equivalent _row_to_dict stays as-is (existing UI consumers may bind to the current shape); operations.py is fresh enough that no consumer depends on synced_at/id/source_table |
| 73 | 2026-05-12 11:55 | (no file change — environment + one-shot data migration) | PG on BF4 was running in UTC-7 (MST) session timezone; existing synced_at values in wbatngl_trip_mirror + hts_heat_mirror were MST-naive | `ALTER DATABASE hmd SET TIMEZONE TO 'Asia/Kolkata';` applied via Python one-liner; backend restarted to flush connection pool; one-shot bulk-shift `UPDATE ... SET synced_at = synced_at + interval '12 hours 30 minutes'` ran on both mirror tables (3907 + 123 rows). Verified pg_now=+05:30, gap=14m (healthy quiet period) | UI's "last sync" label showed timestamps that looked 12.5h stale because PG was in MST, not IST | When PG was originally installed on BF4, default TZ was UTC-7 from the installer; never reconfigured. The OS-clock-12h-behind-IST issue (since fixed by JSW IT) masked the PG TZ issue because both errors stacked | For any future PG instance hosting HMD: run `ALTER DATABASE <dbname> SET TIMEZONE TO 'Asia/Kolkata'` as part of post-create setup. WBATNGL/HTS Oracle sources serve IST-naive timestamps; the mirror must use the same TZ so `func.now()` writes consistently. Memory updated in project_client_pc_context.md |
| 74 | 2026-05-12 14:30 | frontend/package.json + frontend/package-lock.json + frontend/vitest.config.js + frontend/src/test/{setup.js,smoke.test.jsx} | (no frontend test stack) | NEW Vitest + React Testing Library + jsdom test harness; 'test' / 'test:watch' npm scripts; one smoke test file | First frontend tests in the repo — backend has 334 tests; frontend was lint-only | Phase 3 work needs TDD discipline to match backend's quality bar | Five new devDependencies; zero impact on the production bundle |
| 75 | 2026-05-12 15:30 | frontend/src/pages/OperationsLive.jsx + frontend/src/App.jsx + frontend/src/components/Sidebar.jsx + frontend/src/components/OperationsLive/{TopKpiStrip,RecentActivityFeed,ConverterCard,LiveHeatsPanel,ActiveTripsPanel}.jsx + tests for each | (page did not exist) | NEW /operations-live page consuming GET /api/operations-live/dashboard. 1 page + 5 sub-components + ~35 Vitest tests | Page 1 of the Operations Live sprint (Phase 3 of 5) | Per design doc 2026-05-11-operations-live-design.md | 10s polling with document.hidden pause; 1s tick for relative-time label; sidebar entry placed after Live Tracking for all 4 roles (admin/trs/ppc/operator). statusColor reused from torpedoStatus.js for current_status chip on active trips. premium-card / premium-page-container CSS reused — no new design system |
| 76 | 2026-05-12 16:00 | frontend/src/pages/TripHistoryLive.jsx + frontend/src/pages/__tests__/TripHistoryLive.test.jsx + frontend/src/App.jsx + frontend/src/components/Sidebar.jsx | (page did not exist) | NEW /trip-history-live page + /trip-history-live/:trip_id deep-link route. Sidebar entry for all 4 roles, positioned right after Operations Live (uses History lucide icon). URL-state-synced filters via useSearchParams: time_window, source_lab, destination, fleet_id, status, shift, q, page, sort_by, sort_order. 30s list poll + 10s detail poll + 1s tick. ROUTE_CONFIG entries for both /operations-live (Phase 3 final-review carry-over fix) and /trip-history-live | Phase 4 of operations-live sprint — Page 2 (trip history with click-to-expand timeline) | Per design doc 2026-05-11-operations-live-design.md and plan 2026-05-12-operations-live-phase-4.md | New util: none (reuses time.js + torpedoStatus.js from Phase 3). New hook: useTripDetail. New CSS variables: none (uses --success / --warning / --danger / --primary / --text-muted from existing :root, including the same hsl(var / 0.1) alpha syntax Phase 3 Batch E established). The Phase 2 hotfix's renamed key current_torpedo_position.current_status is consumed here for the first time. Phase 4 final commits def8e7b → 992bef4 (13 commits, Batches A–D) |
| 77 | 2026-05-12 16:00 | frontend/src/components/TripHistoryLive/*.jsx + .js + __tests__/*.jsx | (did not exist) | 6 new sub-units: StatusBadge, TripListTable, Pagination, FilterBar, TripStoryExpanded (components) + useTripDetail (hook). 54 new sub-unit Vitest tests (StatusBadge 6 + TripListTable 11 + Pagination 8 + FilterBar 11 + useTripDetail 6 + TripStoryExpanded 12); plus 16 page-integration tests added incrementally across Batches A–D | Coverage for the Phase 4 page | TDD-driven per the plan; component-level tests pass props directly, page-level integration tests mock the api util via vi.mock | Mocking responsibility split: sub-component tests own pure behavior; page tests mock api responses. useTripDetail uses vi.useFakeTimers({ shouldAdvanceTime: true }) to assert 10s polling cadence without real-time waits — `shouldAdvanceTime: true` was needed because plain `vi.useFakeTimers()` also fakes queueMicrotask which makes @testing-library/react's waitFor hang. Batch B's I1 fix (whitespace-trim in updateParams) was folded into Batch C's wiring commit. Final state: 126 frontend tests, 334 backend tests, both green |
| 78 | 2026-05-12 20:55 | docs/plans/2026-05-12-version2-dashboard-design.md | (did not exist) | NEW design doc (~250 LOC) | Approved plan for the new VERSION 2 tab on /statistics, captured during a brainstorm session before any code touched | User asked for a 1:1 port of `desing_idea/dashboard.jsx` into V07 with full live data | Documents the 9 decision points (endpoint-strategy, fleet-derivation, alert-persistence, hybrid endpoints, layout, chart library, theme, role gate, refresh rate), the 4-row grid layout, section→data-source mapping, backend & frontend file structure, 6-phase implementation order, and 6 risk-mitigations |
| 79 | 2026-05-12 20:55 | backend/database/models.py | 596 LOC, last class was HtsHeatMirror | Added `Alert` class (~46 LOC) just above HtsHeatMirror | New persistent store for the V2 dashboard's Alerts & Exceptions feed | Dashboard needs a "last 60 min" alert history with ack workflow — no existing table fits | 13 columns: kind/severity/tag/message/location/torpedo_id/trip_id/source/destination/raw_value/threshold/detected_at/acknowledged_at/acknowledged_by. 2 composite indexes: (kind, torpedo_id, acknowledged_at) for the 30-min dedupe hot path; (detected_at DESC) for feed listing |
| 80 | 2026-05-12 20:55 | backend/alembic/versions/v2dash001_add_alerts_table.py | (did not exist) | NEW Alembic migration | DDL for the `alerts` table | Migration matches the ORM definition | down_revision = 'h1i2j3k4l5m6' (latest head). Creates table + 6 indexes (5 single-column + 2 composite). DESC index on detected_at uses raw `op.execute("CREATE INDEX ... DESC")` since declarative SQLAlchemy can't express column order on indexes. Downgrade drops all indexes then the table |
| 81 | 2026-05-12 20:55 | backend/utils/alert_detector.py | (did not exist) | NEW ~280 LOC | 7 pure detector functions + 2 orchestrators that scan mirror rows and emit Alert payloads | Centralizes threshold checks so the sync jobs stay thin and the rules are unit-testable | Thresholds hard-coded matching the design idea: cold metal <1450 °C, S >0.05 %, Si >1.2 %, dwell >180 min, sms-ack-late >60 min, gps-stale >300 s, battery <60 %. `_has_recent_unacked()` enforces a 30-min dedupe per `(kind, torpedo_id)` so every 60s WBATNGL tick on the same closetime row is a no-op. `scan_wbatngl_rows()` orchestrates chemistry/dwell/no-sms-ack; `scan_fleet_rows()` orchestrates gps-stale (and battery once SuVeechi exposes the column). Caller commits |
| 82 | 2026-05-12 20:55 | backend/utils/wbatngl_trip_sync.py | `pull_and_upsert_from_source()` ended with `stats["upserted"] = upsert_rows(db, mirror_rows); return stats`; `run_once()` total dict had 4 keys | After upsert, call `alert_detector.scan_wbatngl_rows(db, mirror_rows)`, commit if any added, add `alerts_added` to stats (and to the run_once accumulator) | Hook detector into the existing 60s WBATNGL sync without changing any other contract | Sync job is the natural choke point — every cold-metal/high-S sample lands here first | Wrapped in try/except + rollback so a detector regression NEVER breaks the main sync (alerts are downstream). Logged separately from the main fetch/upsert summary |
| 83 | 2026-05-12 20:55 | backend/utils/suveechi_sync.py | `sync_once()` returned bare upsert stats | After upsert_locations(), call `alert_detector.scan_fleet_rows(db)` for GPS-stale detection on the latest fleet rows | Hook detector into the 10s SuVeechi sync | Same rationale as #82 — sync is the natural choke point | Same try/except/rollback pattern as #82. Log line now ends with `alerts+=N`. scan_fleet_rows does its own latest-per-fleet_id subquery, so it's O(53) per tick at most |
| 84 | 2026-05-12 20:55 | backend/routes/v2_dashboard.py | (did not exist) | NEW ~520 LOC, 8 endpoints under `/api/statistics/v2/*` | Backend for the new VERSION 2 dashboard tab | Per design doc — hybrid endpoint shape (one fast aggregate + heavy individuals) | Endpoints: GET /overview (KPIs + fleet-donut + shift-bars; 10s cadence); /throughput?range=24h\|7d\|30d (60s); /sankey (60s); /active-trips?limit=7 (10s); /alerts?window=60m (10s); POST /alerts/{id}/ack; /chemistry-distribution (60s); /system-health (30s, 30s server cache). Fleet donut math: `_fleet_breakdown()` derives 7 buckets by joining FleetManagement.status + active Trip.status phase + MaintenanceSchedule presence. Stage-strip mapping: `_trip_status_to_stage()` maps V07's 16-state TripStatus to the design's 5 stages (Tap/Weigh/Transit/SMS/Return). System health probes Postgres live, but uses MAX(synced_at) freshness on the WBATNGL/HTS/SuVeechi mirrors so we don't pay an Oracle round-trip on every poll. Requires authenticated user — no role gate yet (frontend hides the tab from non-admins) |
| 85 | 2026-05-12 20:55 | backend/main.py | imported statistics/deviation/performance routers; registered them lines 462-464 | Added `v2_dashboard` to the import line + `app.include_router(v2_dashboard.router)` directly after performance_analytics | Wire the new router into the FastAPI app | Standard router-registration pattern (matches existing 30+ routers) | Single line added in each of the import and include sections; positioned alongside other analytics routers |
| 86 | 2026-05-12 20:55 | frontend/src/pages/Statistics.jsx | 2 toggle buttons (PERFORMANCE / DEVIATION); 2 render branches | 3 toggle buttons with VERSION 2 placed **to the LEFT** of PERFORMANCE; new `activeTab === 'v2'` branch renders `<Version2Dashboard />` | Mount the new tab in the existing role-gated toggle | Reuses the existing switcher-tabs + HeaderContext pattern so no styling work needed for the toggle itself | New `Sparkles` icon from lucide-react. Same admin/trs/ppc role gate. Tab order matches user spec: V2 / PERFORMANCE / DEVIATION |
| 87 | 2026-05-12 20:55 | frontend/src/components/Statistics/Version2Dashboard.jsx + Version2Dashboard.css | (did not exist) | NEW container component (~100 LOC) + scoped CSS (~340 LOC) | Hosts the master refresh tick (10s) and lays out the 4-row grid; CSS scopes every V2 token under `.v2-dashboard` so the rest of the app is untouched | Per design doc | Exports `useV2Endpoint(endpoint, params, {tick, cadence})` — a tiny hook used by every section. Each section opts into a refresh cadence (1=10s, 3=30s, 6=60s) by passing `cadence`. CSS aliases V07's `--card-bg / --main-bg / --text-primary / --border-color / --accent` to private `--v2-*` vars + adds two extra hues the design uses (cyan #06b6d4, violet #a78bfa). Light theme only — no dark-mode variants. Layout: row-1 `repeat(6, 1fr)` KPI grid, row-2 `1.05fr 1.45fr 1.35fr`, row-3 `1.7fr 1fr`, row-4 `1.4fr 1fr 1fr`. Responsive break at 1280px drops to 2-col, 900px drops to 1-col |
| 88 | 2026-05-12 20:55 | frontend/src/components/Statistics/V2/*.jsx | (did not exist) | NEW 11 section components (~900 LOC total): KPIRow, KPICard, KPIBig, FleetDonut, ThroughputChart, FlowSankey, StageDots, ActiveTripsTable, AlertFeed, ShiftBars, ChemHistogram, SystemHealth | 1:1 visual port of `desing_idea/dashboard.jsx` with V07 light theme | Per design doc section-by-section breakdown | Recharts for AreaChart (Throughput), PieChart (FleetDonut), BarChart (ChemHistogram). Hand-rolled SVG for the Sankey ribbons (math lifted from the design idea verbatim) and the KPIBig sparkline (24h tonnes/hour). AlertFeed has an inline Acknowledge button that POSTs to /alerts/{id}/ack and dims the row optimistically. FleetDonut uses 7 colors that exactly match the design idea palette. StageDots is shared between the trip table and (later) any other timeline view |
| 89 | 2026-05-12 20:55 | frontend build | `npm run build` baseline ~146 kB Statistics chunk | `Statistics-cq0zW28_.js` now 158.95 kB / 29.77 kB gzip | Verify the new module integrates cleanly | Sanity check before handover | `npx vite build` → ✓ built in 8.45s, all chunks generated, no errors, no warnings beyond the existing `no-unused-vars` false-positives on JSX-only identifiers (same pattern as the existing AdminStatistics.jsx) |
| 90 | 2026-05-12 22:15 | docs/plans/2026-05-12-livetracking-v2-design.md | (did not exist) | NEW design doc (~210 LOC) | Approved plan for the new VERSION 2 tab on the Live Tracking page | User wants the same V1/V2 toggle pattern as Statistics applied to the map page, with a 1:1 layout port of `desing_idea/tracking.jsx` | Captures 13 design decisions (asset-data-gap=D′, layout=3-col with conditional detail, close X top-LEFT, re-click same-id is no-op, selected highlight in list + map, design-idea marker visual, Leaflet kept not replaced, V1 file left untouched, header toggle pattern, YARD/REPAIR seed strategy, /torpedoes returns all 53, 5s + 10s refresh tiers), the 4-row implementation plan, and 6 risk mitigations including a "push to BOTH origin and new-origin" reminder learned from V2 dashboard deploy |
| 91 | 2026-05-12 22:15 | backend/database/models.py | FleetLiveLocation had 5 columns (id, fleet_id, type, x, y, last_updated) | Added `location_text VARCHAR(255) NULL` — captures SuVeechi's `vw_unit_status_ist.location` field that we currently drop | Asset gap analysis (`report.txt` audit) found this field sitting in the SuVeechi feed unused. Design idea uses it prominently in the right-panel LOCATION section ("At HMY2 - Corex Point No.125", "0.08 KM NW of LRS1") | Detected via audit of `test_db_inventory.py` output | Column nullable for back-compat — existing rows stay null until next 10s tick re-upserts them with the new value. Single column add, no compound indexes |
| 92 | 2026-05-12 22:15 | backend/alembic/versions/livetrack001_add_location_text_and_seed_nodes.py | (did not exist) | NEW Alembic migration | Adds the new `location_text` column AND seeds YARD/REPAIR rows in `locations_coordinates` + backfills null x/y on weighbridges | down_revision = 'v2dash001' (alerts migration from earlier today) | Three idempotent operations: (1) `op.add_column` for location_text on fleet_live_locations; (2) UPDATE weighbridges SET x/y WHERE name IN (9 alternate WB names) AND x IS NULL — handles whatever the admin labelled the WB rows with; (3) two `INSERT … WHERE NOT EXISTS` for YARD and REPAIR rows in locations_coordinates using new `type` values ('yard', 'repair'). Coords from `desing_idea/tracking.jsx` PLANT_GEO. Downgrade drops the seeded rows and the column |
| 93 | 2026-05-12 22:15 | backend/utils/suveechi_sync.py | `upsert_locations()` stored 5 fields per FleetLiveLocation; SuVeechi's `location` column was fetched but never persisted | Now captures `location` field, strips trailing `*` and whitespace (sample rows in `report.txt` showed `"At HMY2 - Corex Point No.125*"`), stores on every upsert | Wire the location text into the new column added in #91/#92 | Required for design idea's LOCATION panel and the truncated location preview in the left list | Cleaning logic: `str(raw).strip().rstrip('*').strip()` — handles trailing junk seen in the dump. Empty string becomes None so the frontend can show "—". Backwards-compatible: existing rows pre-migration stay null until next sync tick |
| 94 | 2026-05-12 22:15 | backend/routes/tracking_v2.py | (did not exist) | NEW ~340 LOC, 3 endpoints under `/api/tracking/v2/*` | Backend for the new VERSION 2 Live Tracking tab | Hybrid endpoint shape — one aggregated list + per-torpedo detail + cached node list | Endpoints: GET /torpedoes (5s; per-torpedo derived 7-state status, lat/lon, location_text, last_temp, last_report_sec, destination for active-trip transit line); GET /torpedoes/{fleet_id} (10s when panel open; header + location + current_trip with 5-stage idx + chemistry + asset {life_cycles=COUNT WBATNGL rows, campaign=floor(life_cycles/80)+1, shell_temp/heel/gps_battery=null with parity-only fields kept for UI} + recent_trips last 5); GET /plant-nodes (5min server cache; merges LocationCoordinate BFs/SMSs/YARD/REPAIR with Weighbridge rows, kind ∈ {bf,sms,wb,yard,repair}). `_derive_status` is the per-torpedo version of V2 dashboard's `_fleet_breakdown` logic — same 7 buckets so list/donut/map all agree |
| 95 | 2026-05-12 22:15 | backend/main.py | tracked imports + registrations from earlier today (v2_dashboard, etc.) | Added `tracking_v2` to imports and `app.include_router(tracking_v2.router)` after v2_dashboard | Wire new router into FastAPI app | Standard pattern (matches v2_dashboard and the 28 other routers) | Single line added to import line, single line added to include section |
| 96 | 2026-05-12 22:15 | frontend/src/pages/LiveTracking.jsx | (did not exist) | NEW thin wrapper (~75 LOC) | Hosts the V1/V2 toggle via HeaderContext; default tab 'v1' so existing operators see no change until they click VERSION 2 | Per design doc decision #9 — V1 stays untouched in Dashboard.jsx, new wrapper added | Imports `Dashboard` eagerly and `LiveTrackingV2` lazily; HeaderContext gets `left` (page-title-left span), `center` (.switcher-tabs with Sparkles/LayoutDashboard icons matching Statistics page exact pattern), `forceLeftTitle: true` so the header's centered default title is suppressed. Cleanup `useEffect` returns the standard null-everything teardown |
| 97 | 2026-05-12 22:15 | frontend/src/App.jsx | Route `/` → `<Dashboard />` directly; `Dashboard` imported via lazy() | New lazy `LiveTracking` import + route `/` now → `<LiveTracking />`. Dashboard import retained for the wrapper to use | Mount the toggle wrapper as the live-tracking entry point | Per design doc decision #9 | One-line edit in two places — `const LiveTracking = lazy(...)` added, route element swapped. V1 component file Dashboard.jsx is byte-for-byte unchanged |
| 98 | 2026-05-12 22:15 | frontend/src/pages/LiveTrackingV2.jsx | (did not exist) | NEW container (~110 LOC) | 3-column grid container that hoists `selectedFleetId/filter/search/tick` state and fans data to children | Per design doc Section 3 | Master 5s tick `setInterval`; fetches `/torpedoes` once per tick (gated by inflight ref to avoid overlap) and `/plant-nodes` once on mount. Filtered torpedoes derived with `useMemo` (search across fleet_id + location_text, plus 7-state filter). `handleSelect` uses `useCallback` and ignores same-fleet_id re-clicks (Q3 answer 2=a from brainstorm). CSS class `panel-open` toggles the grid template columns between `270px 1fr` and `270px 1fr 360px` |
| 99 | 2026-05-12 22:15 | frontend/src/components/LiveTrackingV2/TorpedoListPanel.jsx | (did not exist) | NEW left column (~155 LOC) | 270px torpedo list with search + 8 filter pills (All + 7 derived statuses) + scrollable list of memoized rows | Per design doc Section 5.3 | TorpedoRow is `React.memo`'d with custom equality — only re-renders on selected/status/temp/location/age changes. Pattern lifted from V1's FleetMarker memo (tracker #34) so 53 rows × 5s polling doesn't cause a re-render storm. Exports STATUS_COLORS map (used by PlantMap and TorpedoDetailPanel) so all three modules share one source of truth for the 7-state color palette |
| 100 | 2026-05-12 22:15 | frontend/src/components/LiveTrackingV2/PlantMap.jsx | (did not exist) | NEW center column (~310 LOC) | Leaflet map with design-idea visuals on top | Per design doc Section 5.4 + V2 marker visual | Reuses V1's tile layer (Esri World Imagery). NEW: station rectangles as `L.divIcon`s with kind-specific border color, dashed `L.polyline` track edges between station pairs (hardcoded TRACK_EDGES — topological, not geo), torpedo divIcons showing colored dot + TLC number above (matches design idea), pulsing ring CSS keyframe when fleet is selected, animated dashed transit line from torpedo→destination station for "In Transit" buckets. Marker `_torpedoIconCache` keyed by `(fleet_id, status, isSelected)` — bounded ~3 entries per fleet, GC-safe. `TorpedoMarker` is `React.memo`'d with full custom equality. `InvalidateOnResize` subcomponent calls `map.invalidateSize()` 320ms after `panelOpen` toggles (matching the CSS transition duration). `CenterMapListener` subcomponent listens for `window` CustomEvent `v2track:centerMap` from the detail panel's button and calls `map.flyTo([lat,lon], 16)`. Bottom-left STATIONS legend + top-right live stats card (Loading/In Transit/At SMS counts + "Live · 5s" pulse dot) |
| 101 | 2026-05-12 22:15 | frontend/src/components/LiveTrackingV2/TorpedoDetailPanel.jsx | (did not exist) | NEW right column (~250 LOC) | 360px slide-in panel with 7 sections | Per design doc Section 5.5 — 1:1 with desing_idea/tracking.jsx detail panel | Close button (X) positioned **top-LEFT** per explicit user instruction (unconventional but requested). 7 sections: header (X + TLC id + status + age), Location (text + lat/lon), Current Trip (with 5-stage horizontal strip when active trip exists, else "No active trip"), Chemistry & Temp (3 colored metrics — red <1450, amber 1450-1470 for temp; amber >0.05 for sulfur), Asset (life_cycles, campaign, + 3 sensor-placeholder rows with AlertCircle "no sensor" tooltip), Recent Trips (last 5 WBATNGL rows compact), Footer (Center on map → fires `v2track:centerMap` CustomEvent, Export → builds CSV client-side from recent_trips and triggers download). Polls /api/tracking/v2/torpedoes/{fleet_id} immediately on fleet_id change + every other tick (10s). `lastFetchedFleetRef` prevents loading flash when same fleet polls |
| 102 | 2026-05-12 22:15 | frontend/src/components/LiveTrackingV2/LiveTrackingV2.css | (did not exist) | NEW ~440 LOC scoped CSS | All V2 Live Tracking tokens scoped under `.v2-tracking` so the rest of the app is untouched | Light-theme adaptation of design idea's dark theme — same proportions, V07 colors | Aliases V07's `--card-bg / --main-bg / --text-primary / --border-color / --accent` to private `--v2t-*` vars plus 5 extras (amber/red/green/cyan/violet). Grid morph: `grid-template-columns: 270px 1fr` ↔ `270px 1fr 360px` with `transition: 280ms cubic-bezier(0.4, 0, 0.2, 1)`. 4 keyframe animations: pulse-anim (selected torpedo ring), dash (in-transit polyline), blink (live dot), slide-in (detail panel mount). Responsive break at 1280px shrinks list to 240px + detail to 320px; at 1024px detail panel becomes absolute-positioned overlay instead of squishing the map. Page-title-left styled as Space Grotesk 1.25rem bold |
| 103 | 2026-05-12 22:15 | frontend build | Statistics chunk 158.95 kB from earlier today | NEW chunk `LiveTrackingV2-B4Sz4Bpn.js` 18.93 kB / 5.78 kB gzip (lazy-loaded); Dashboard chunk unchanged at 24.69 kB | Verify the new module integrates cleanly without disturbing V1 | Sanity check before handover | `npx vite build` → ✓ built in 8.52s, all chunks generated, no errors. V1 Dashboard.jsx file is byte-for-byte untouched so its chunk should be identical between before and after — confirmed Dashboard chunk size unchanged at 24.69 kB pre- and post-V2 work |

## Net Change

### Backend
- **LOC:** 18,402 → 18,127 (−275 LOC, −1.5%)
- **Files deleted:** 1 (`backend/routes/geofence.py`)
- **Files created:** 1 (`backend/utils/analytics_helpers.py`)
- **Files modified:** 8 (main.py, models.py, daily_plans.py, statistics.py, performance_analytics.py, plans.py, CLAUDE.md, trip_crud.py)

### Frontend
- **LOC:** 35,320 → 33,655 (−1,665 LOC, −4.7%)
- **Files deleted:** 4 (AdminPlanning.jsx, LiveOperations.jsx, PlanningHistory.jsx, App.css)

### Combined
- **Total LOC saved:** −1,940 LOC

## Verification

- `python -c "from backend.main import app; print('OK')"` → OK
- No test files reference deleted models
- All 24 routers (was 25) registered correctly
- Tests: 166/166 passing (after #11 fix; was 165/166)
- Commit `0918ef55` pushed to `origin/feature/strip-security-to-local-auth`
- `npm run build` → OK in 8.12s, all chunks generated
| 104 | 2026-05-12 23:15 | frontend/src/pages/LiveTracking.jsx | Set `left: <div className="page-title-left">Live Tracking</div>` + `forceLeftTitle: true` | Removed the `left` field entirely; kept only `center` (the toggle) | Header was rendering "LIVE TRACKING" (route auto-title from App.jsx ROUTE_CONFIG, CSS-uppercased) AND "Live Tracking" (my custom left content) side-by-side on V2 page | Header.jsx renders its auto title in the left slot AS SOON AS `center` is non-null OR `forceLeftTitle:true` — providing our own `left` produces a duplicate | Same pattern as Statistics.jsx (which sets only `center`). Title comes from `ROUTE_CONFIG['/'].title = 'Live Tracking'` |
| 105 | 2026-05-12 23:20 | backend/alembic/versions/v2dash001_add_alerts_table.py | `down_revision = 'h1i2j3k4l5m6'` | `down_revision = 'c3e8d219a4b1'` | `python -m alembic upgrade head` on BF4 failed with "Multiple head revisions are present" — Alembic refused to upgrade because the migration tree had two unmerged heads | I picked the wrong parent for v2dash001 — `h1i2j3k4l5m6` already had a child (`8ccb1a387ca7` from the WBATNGL trip mirror chain) so my new migration created a branch instead of extending the chain. The actual latest pre-existing head at sprint time was `c3e8d219a4b1` (add_fleet_live_locations_fleet_id_index, shipped 11-May per tracker #c3e8d219a4b1) | Re-parented v2dash001 to c3e8d219a4b1. Chain is now linear: c3e8d219a4b1 → v2dash001 → livetrack001. Safe to apply even if v2dash001 was already applied earlier — Alembic uses `alembic_version` table to track applied revisions by ID, not by parent link, so existing applications are unaffected by parent change |
| 106 | 2026-05-12 23:50 | docs/plans/2026-05-12-trip-operations-v2-design.md | (did not exist) | NEW design doc (~180 LOC) | Approved plan for replacing the JSW tab content on Trip Management with a 1:1 port of `desing_idea/trips.jsx` (4-sub-tab Trip Operations board), plus three workflow changes: default-tab flips to V2 across Statistics/LiveTracking/TripMgmt, sidebar reorder so Dashboard/LiveTracking/TripMgmt sit 1/2/3, and hiding 3 pages from sidebar | Sister sprint to V2 Dashboard + V2 Live Tracking shipped earlier today | Captures 14 design decisions, file structure, backend changes, frontend layout for all 4 sub-tabs, risk matrix |
| 107 | 2026-05-12 23:50 | backend/routes/jsw.py | `/api/jsw/trips` returned plain WBATNGL rows; chemistry/dwell/etc. fields ungated | Added `mode` query param (in_flight / completed / omit); added `_derive_stage_idx()` mapping WBATNGL timestamps to 5-stage strip; added `_fetch_alerts_by_trip_id()` for batch alert lookup; new `_row_to_dict_v2()` includes `stage_idx` + `alert` fields | Trip Operations V2 cards need a stage indicator and a per-trip alert tag in a single round-trip | Per design doc decision #1 (option A — reuse `/api/jsw/trips` + add 2 new) | `mode='in_flight'` filter: out_date NOT NULL AND sms_ack_time IS NULL. `mode='completed'` filter: sms_ack_time NOT NULL. Both stack on top of existing time_window. New `_fetch_alerts_by_trip_id` does ONE query for all returned trip_ids → no N+1. Old callers (current JswTripsTab.jsx) ignore unknown fields, so this is safe additive |
| 108 | 2026-05-12 23:50 | backend/routes/jsw.py | (did not exist) | NEW `GET /api/jsw/v2/exceptions` (~80 LOC) | Alerts queue for the Exceptions sub-tab | Backend gap identified in design — V07 had no endpoint that joined alerts and wbatngl_trip_mirror | LEFT JOIN `alerts` ↔ `wbatngl_trip_mirror` on trip_id. Window default 24h (bounded to 7 days). Optional `kind` filter (cold/chem_s/chem_si/dwell/gps_stale/sms_ack/battery). Returns severity-tagged rows with trip context (tap_no/source_lab/destination/net_weight/temp). Ack workflow reuses existing /api/statistics/v2/alerts/{id}/ack from v2 dashboard |
| 109 | 2026-05-12 23:50 | backend/routes/jsw.py | (did not exist) | NEW `GET /api/jsw/v2/timeline?hours=12` (~75 LOC) | Per-torpedo gantt for Timeline sub-tab | Backend gap identified in design | Single SELECT over WBATNGL rows where first_tare_time OR updated_date in window. Groups by fleet_id client-side after fetch — for the small set of in-window rows (~30-100), grouping in Python is cheaper than two queries. Picks top `lanes_limit` fleets by trip count (defaults to 18, matches design idea). Joins FleetManagement.status for the lane label. Trip end=null for in-flight rows so the frontend can draw a "ghost" bar from start to now |
| 110 | 2026-05-12 23:50 | frontend/src/pages/Statistics.jsx | `useState('performance')` | `useState('v2')` | Default tab change requested by user — land on V2 first | Sister to LiveTracking + TripManagement default flips | Performance and Deviation toggles still visible in header, one click away. No data fetched until user clicks back |
| 111 | 2026-05-12 23:50 | frontend/src/pages/LiveTracking.jsx | `useState('v1')` | `useState('v2')` | Same user request | Sister flip | V1 still mounted on click; Dashboard.jsx body unchanged |
| 112 | 2026-05-12 23:50 | frontend/src/pages/TripManagement.jsx | `useState('overview')` AND `{activeTab === 'jsw' && <JswTripsTab />}` | `useState('jsw')` AND `{activeTab === 'jsw' && <JswTripOperations />}`; new import for JswTripOperations | Lands on new design first; JSW main tab content swapped from legacy table view to the new 4-sub-tab board | Per design doc — JSW tab content is the actual V2 redesign target | Old `<JswTripsTab />` import and file kept in tree as fallback; not mounted from UI |
| 113 | 2026-05-12 23:50 | frontend/src/components/Sidebar.jsx | 4 role arrays with paths in old order, all including /operations-live + /trip-history-live + /plant | Reordered all 4 arrays so Dashboard/Live Tracking/Trip Management sit at positions 1/2/3 (where applicable per role); REMOVED 3 paths from all roles (operations-live, trip-history-live, plant) — routes stay defined in App.jsx, only sidebar nav hides them | User said: hide these 3 pages for all users; future decision pending whether to reuse or remove entirely | Per design doc decision #9 and memory `project_hidden_sidebar_pages.md` | New role counts: Admin 9 (was 12), TRS 8 (was 11), PPC 4 (was 7), Operator 6 (was 8). Pages/files NOT deleted — restored later by reverting this single array change. Inline comment in Sidebar.jsx points future readers to the memory note |
| 114 | 2026-05-12 23:50 | frontend/src/components/JswTripOperations/JswTripOperations.jsx | (did not exist) | NEW container (~145 LOC) | Trip Operations V2 shell — 4 sub-tabs + filter chips (Shift/Source/Destination) + Export button | Per design doc Section 3 | Master 10s tick at parent level; each sub-tab consumes the same tick but only the mounted sub-tab fetches. setCount(id, n) callback so sub-tabs can populate the badge counts in the tab row. FilterChip is a native-select dropdown for keyboard/a11y |
| 115 | 2026-05-12 23:50 | frontend/src/components/JswTripOperations/ActiveTripBoard.jsx | (did not exist) | NEW (~210 LOC) — left-cards + right-detail-pane layout | 1:1 with design idea Active Trip Board | Per Section 4.1 | `/api/jsw/trips?mode=in_flight&time_window=today&page_size=50` source. TripCard is `React.memo`'d with custom equality (only re-renders on stage_idx/temp/s_l/si_l/net_weight/alert.id changes) — mirrors V1's FleetMarker memo pattern (tracker #34). Filter values mapped to backend query params. Selected card → opens TripDetailPane with full row |
| 116 | 2026-05-12 23:50 | frontend/src/components/JswTripOperations/TripDetailPane.jsx | (did not exist) | NEW (~270 LOC) — 8 sections | Right-side detail pane | Per Section 4.2 | Sections: header / quick meta (Ladle/Source/TH/Dest/Shift) / Lifecycle (5 vertical stages with timestamps + sub-line) / Weights (3-col) / Chemistry (4-col with red <1450 temp + amber >0.05 sulfur) / Alert banner (Ack POST to /api/statistics/v2/alerts/{id}/ack — shared with V2 dashboard) / Audit Log (client-side derived from row timestamps) / Footer actions (Acknowledge SMS / Flag Issue / Download CSV). Audit log uses isoOrNaiveToHHMM helper for consistent HH:mm format |
| 117 | 2026-05-12 23:50 | frontend/src/components/JswTripOperations/ExceptionsQueue.jsx | (did not exist) | NEW (~155 LOC) — alerts queue table | Per Section 4.3 | Source: /api/jsw/v2/exceptions. Filter pills (All / Cold metal / Chemistry S / Chemistry Si / Dwell / GPS). Optimistic ack update — row dims instantly without waiting for next 10s tick. Dwell computed client-side from first_tare_time to sms_ack_time (or now() if still in-flight). Reports the unacked-count to parent via setCount('exceptions', n) so the red badge in the sub-tab row updates |
| 118 | 2026-05-12 23:50 | frontend/src/components/JswTripOperations/CompletedTable.jsx | (did not exist) | NEW (~155 LOC) — completed-trips paginated table | Per Section 4.4 | Source: /api/jsw/trips?mode=completed&time_window=today. 13 columns: Tap / Ladle / TH / Route / Net / Temp BF / ΔT / S / Si / Tap→Out / Out→Ack / Cycle / Shift. ΔT = temp - bds_temp (null-safe). Server pagination 50/page. setCount('completed', total) → badge in sub-tab row |
| 119 | 2026-05-12 23:50 | frontend/src/components/JswTripOperations/GanttView.jsx | (did not exist) | NEW (~175 LOC) — per-torpedo timeline gantt | Per Section 4.5 | Source: /api/jsw/v2/timeline?hours=12. Hand-rolled — no chart lib (Recharts has no gantt). 110px sticky lane label column + 1fr time-axis track. Hour ticks computed in useMemo from cutoff/now span. Each trip bar positioned by start/end as % of total window; in-flight (end=null) draws a hatched "ghost" bar to now. Color by destination (SMS1=blue, SMS3=cyan, SMS4=violet, RFL=green). HOURS_OPTIONS pills (6h/12h/24h) re-fetch with different `hours` param |
| 120 | 2026-05-12 23:50 | frontend/src/components/JswTripOperations/JswTripOperations.css | (did not exist) | NEW ~520 LOC scoped CSS | Per design doc Section 5 — light-theme adaptation of design idea | All tokens scoped under `.jsw-trip-ops` so the rest of TripManagement (and the app) is untouched. Aliases V07 `--card-bg / --main-bg / --text-primary / --border-color / --accent` to private `--jto-*` vars plus 5 extra (amber/red/green/cyan/violet). Selected card: blue border + tinted bg + soft shadow. Stage strip: horizontal 5-step with kind-based bg color. Gantt: 110px sticky labels + position:absolute bars with hatched .ghost variant for in-flight |
| 121 | 2026-05-12 23:50 | frontend build | TripManagement chunk 150.81 kB pre-V2 | TripManagement-C6kQrgvu.js now 165.76 kB / 36.05 kB gzip | Verify all 6 new components + extended jsw.py integrate cleanly | Sanity check before handover | `npx vite build` → ✓ built in 10.34s, all chunks generated, no errors |
| 122 | 2026-05-13 00:35 | docs/plans/2026-05-13-weighbridge-audit-design.md | (did not exist) | NEW design doc (~150 LOC) | Approved plan for a new Weighbridge Audit page at sidebar position 4 (admin/TRS only) that cross-checks WBATNGL net_weight vs net_weight_actual to surface variance + drift per weighbridge | User pivoted from Chemistry Analytics to Weighbridge Audit — same sidebar slot, smaller scope | Captures 10 design decisions including the data-gap fix (extend WbatnglTripMirror to capture net_weight_actual + tare_weight_actual columns that already exist upstream in Oracle but were being dropped on ingest) |
| 123 | 2026-05-13 00:35 | backend/database/models.py | WbatnglTripMirror had gross/tare/net_weight + chem columns; SMS-side actual readings were dropped on sync | Added `net_weight_actual` and `tare_weight_actual` columns (both Float NULLABLE) just below `net_weight` | Audit page needs to compute variance % = (net_wb - net_sms) / net_wb × 100; SMS-side actuals are the second half of that equation | Identified during design from report.txt audit — TARE_WEIGHT_ACTUAL and NET_WEIGHT_ACTUAL are in Oracle but we never mirrored them | Both nullable so existing rows pass; next 60s WBATNGL sync tick re-upserts and fills in. Inline comment cross-refs the audit page design doc |
| 124 | 2026-05-13 00:35 | backend/alembic/versions/wbaudit001_add_actual_weights_to_wbatngl_mirror.py | (did not exist) | NEW migration | DDL for the 2 new nullable columns | Required to match the ORM definition in #123 | down_revision = 'livetrack001' (clean linear chain: c3e8d219a4b1 → v2dash001 → livetrack001 → wbaudit001). Adding columns (not tables) so init_db().create_all() won't auto-add them and won't collide with Alembic on BF4 — no `alembic stamp` dance this time |
| 125 | 2026-05-13 00:35 | backend/utils/wbatngl_trip_sync.py | `row_to_mirror_dict()` produced gross/tare/net + chem fields; ignored TARE_WEIGHT_ACTUAL and NET_WEIGHT_ACTUAL from Oracle | Added `net_weight_actual` and `tare_weight_actual` to the upsert dict, each wrapped in `_zero_to_null()` so a 0 reading (Oracle's "no value yet") becomes NULL instead of a fake +100% variance | Capture the upstream fields we were dropping | Per design doc decision #1 (Option A) | Single edit; backwards compatible — existing dict keys unchanged. Next WBATNGL sync tick (within 60s of deploy) re-upserts every recent trip with the actuals filled in |
| 126 | 2026-05-13 00:35 | backend/routes/weighbridge_audit.py | (did not exist) | NEW ~250 LOC route module with 2 endpoints under /api/weighbridge-audit/v2/* | Backend for the new Weighbridge Audit page | Per design doc | `GET /overview?range=today\|shift_a\|7d` returns 4 KPIs (trips_reconciled / open_variances / avg_variance_pct / total_dispatched_kt) + 11-bin variance histogram (-0.6 to +0.6 step 0.2) + 3 per-WB calibration rows with 30-day drift %. `GET /log?range=&filter=all\|variance\|pending&limit=24` returns table rows. Helper `_wb_from_source` maps BF3/BF4/BF5 to WB HMY1/HMY2/LRS1 (heuristic matching design idea track edges). 60s cache on overview, 30s on log |
| 127 | 2026-05-13 00:35 | backend/main.py | imports + registrations for V2 dashboard / Live Tracking V2 from prior sprints | Added `weighbridge_audit` to import line + `app.include_router(weighbridge_audit.router)` after tracking_v2 | Wire new router into FastAPI app | Standard pattern | Two line additions; positioned alongside the other V2 routes |
| 128 | 2026-05-13 00:35 | frontend/src/pages/WeighbridgeAudit.jsx | (did not exist) | NEW page container (~95 LOC) | Hosts the 60s tick, range/filter state, and HeaderContext for the right-side pills + Export button | Per design doc Section 3 | Default range='today', default filter='all'. HeaderContext.right injects [Shift A] [Today] [7d] pills + Export. Master setInterval ticks every 60s; child components fetch on each tick (server caches 60s so triple-fetch is cheap). Body layout: KPIRow on top, then grid-2col with WeighbridgeLog on left and (VarianceHistogram stacked over CalibrationCard) on right |
| 129 | 2026-05-13 00:35 | frontend/src/components/WeighbridgeAudit/KPIRow.jsx | (did not exist) | NEW 4 KPI cards | TRIPS RECONCILED / OPEN VARIANCES / AVG VARIANCE / TOTAL DISPATCHED | Per design idea + design doc | Each card: label / icon top-right / big mono value / unit / sub-line. Tone driven by server: open_variances goes amber if >0. Shares the /overview endpoint with VarianceHistogram + CalibrationCard — server cache makes this efficient |
| 130 | 2026-05-13 00:35 | frontend/src/components/WeighbridgeAudit/WeighbridgeLog.jsx | (did not exist) | NEW ~120 LOC log table | 10-column table: Time / WB / Trip / Ladle / Gross / Tare / Net (WB) / Net (SMS) / Var / Status. 3 filter pills (All / Variance ≥ 0.3% / Pending recon) | Per design idea | Source: /api/weighbridge-audit/v2/log. Rows React.memo'd with custom equality (only re-render when displayed fields change) — same anti-regression discipline as the Trip Operations V2 card memo (tracker #115). Var cell colored amber when |var| ≥ 0.3%. Status tag green for OK, amber for Review |
| 131 | 2026-05-13 00:35 | frontend/src/components/WeighbridgeAudit/VarianceHistogram.jsx | (did not exist) | NEW Recharts BarChart with 11 bins | -0.6 to +0.6 (step 0.2) | Per design idea | Bar fill by server-supplied tone (green for in-spec ≤0.2, amber 0.3-0.4, red ≥0.5). Bin counts rendered above bars via Recharts LabelList. No animation (isAnimationActive=false) for stable rendering. Shares /overview endpoint |
| 132 | 2026-05-13 00:35 | frontend/src/components/WeighbridgeAudit/CalibrationCard.jsx | (did not exist) | NEW ~95 LOC card | 3 rows (WB HMY1 / HMY2 / LRS1) with 30-day drift % + Recalibrate button | Per design idea | "last cal" shows "—" (no calibration log table yet — Option A per design doc). Sample size (`n=N trips`) shown in subtle text. Recalibrate button is toast-only for now — persistence intentionally deferred per design doc decision #8. Toast renders inside the card via local state |
| 133 | 2026-05-13 00:35 | frontend/src/components/WeighbridgeAudit/WeighbridgeAudit.css | (did not exist) | NEW ~250 LOC scoped CSS | Light-theme adaptation of design idea | All tokens scoped under `.wb-audit` so the rest of the app is untouched. Grid 2-col: 1.6fr / 1fr (log left, side-stack right); collapses to single column under 1280px. KPI grid is repeat(4, 1fr) shrinking to 2 then 1 col. Bar/tag/button/pill primitives all defined locally — no global pollution |
| 134 | 2026-05-13 00:35 | frontend/src/App.jsx | imports + ROUTE_CONFIG + routes from prior sprints | Lazy import `WeighbridgeAudit`; added `'/weighbridge-audit': { title: 'Weighbridge Audit' }` to ROUTE_CONFIG; added Route inside the isAdminOrTRS block | Mount the new page at /weighbridge-audit (admin/TRS only — operators/PPC don't audit weighbridges) | Standard route-add pattern matching other V2 pages | Three-line edit |
| 135 | 2026-05-13 00:35 | frontend/src/components/Sidebar.jsx | Admin + TRS menu arrays did not have Weighbridge Audit; imported Lucide icons did not include Scale | Added Weighbridge Audit at position 4 in both admin and TRS arrays (directly after Trip Management per user request); added Scale to the lucide-react import | Surface the new page in the sidebar nav | Per design doc decision #3 | PPC and Operator menus intentionally unchanged — those roles don't have audit responsibilities. Inline comment notes the position-4 placement decision |
| 136 | 2026-05-13 00:35 | frontend build | TripManagement chunk 165.76 kB from prior sprint | `npx vite build` → ✓ built in 8.33s, new WeighbridgeAudit chunk generated, all other chunks size-stable | Verify the new module integrates cleanly without disturbing prior V2 work | Sanity check before handover | No errors, no new lint regressions beyond the existing JSX no-unused-vars false positives |
| 137 | 2026-05-13 01:00 | backend/routes/weighbridge_audit.py | `_wb_from_source()` mapped BF3/BF4/BF5 only; COREX1/COREX2 fell through to None | Added COREX1 → "WB HMY1" and COREX2 → "WB HMY2" mappings | data audit showed ~12K historical Corex trips in WBATNGL not getting a WB label on the audit page | Polish requested by user after Weighbridge Audit shipped | Heuristic split (HMY1 / HMY2) — one sample row's location text literally said "At HMY2 - Corex Point No.125" so Corex sits in the HMY area. Per-WB drift now auto-rolls Corex variance into HMY1 / HMY2 since the drift loop already calls `_wb_from_source()`. Single source of truth — change once if JSW confirms different routing |
| 138 | 2026-05-13 01:00 | frontend/src/components/JswTripOperations/JswTripOperations.jsx | Source filter dropdown: ['All BFs', 'BF3', 'BF4', 'BF5'] · default = 'All BFs' | Source filter dropdown: ['All', 'BF3', 'BF4', 'BF5', 'COREX1', 'COREX2'] · default = 'All' | User added Corex to the list of producers we surface | Same polish as #137 | Default label changed from 'All BFs' → 'All' since the list isn't only BFs anymore. ActiveTripBoard.jsx + CompletedTable.jsx updated in #139 to accept both labels for backwards compat |
| 139 | 2026-05-13 01:00 | frontend/src/components/JswTripOperations/{ActiveTripBoard,CompletedTable}.jsx | Source filter mapping checked `filters.source !== 'All BFs'` | Check `filters.source !== 'All' && filters.source !== 'All BFs'` (accepts both labels) | Match the relabel in #138 without breaking any in-flight sessions whose state still holds 'All BFs' | Polish | Defensive double-check — minor edit, same line in both files |
| 140 | 2026-05-13 01:00 | frontend build | TripManagement chunk 165.76 kB from prior sprint | `npx vite build` → ✓ built in 8.71s, all chunks size-stable | Verify Corex polish builds cleanly | Sanity check | No errors |
| 141 | 2026-05-13 01:10 | backend/routes/weighbridge_audit.py | `_wb_from_source()` mapped BF3/BF4/BF5/COREX1/COREX2 only; BF1+BF2 (historical, ~23K trips) fell through to None | Added BF1 → "WB HMY1" and BF2 → "WB HMY2" mappings; reworked the docstring to spell out the mapping rationale across all 7 source types | User wants legacy BF1/BF2 surfaced for historical analysis on Weighbridge Audit + Trip Operations V2 | Polish requested by user after Corex polish | Pattern: odd numbers (1, 3, COREX1) → HMY1, even numbers (2, 4, COREX2) → HMY2, except BF5 which is geographically separate near LRS1. Per-WB drift auto-rolls BF1/BF2 variance into HMY1/HMY2 via the same `_wb_from_source` loop as Corex |
| 142 | 2026-05-13 01:10 | frontend/src/components/JswTripOperations/JswTripOperations.jsx | Source filter dropdown: ['All', 'BF3', 'BF4', 'BF5', 'COREX1', 'COREX2'] | Source filter dropdown: ['All', 'BF1', 'BF2', 'BF3', 'BF4', 'BF5', 'COREX1', 'COREX2'] | Surface BF1/BF2 alongside BF3-5 + Corex in the Source filter | Same polish as #141 | Backend already accepts any source_lab string, so this is purely UI |
| 143 | 2026-05-13 01:10 | frontend build | TripManagement chunk size from prior sprint | `npx vite build` → ✓ built in 8.36s, all chunks size-stable | Verify BF1/BF2 polish builds cleanly | Sanity check | No errors |
| 144 | 2026-05-13 02:30 | backend/database/models.py | HtsHeatMirror was the only HTS mirror; 4 other HTS Oracle tables (~32K rows total) had no local mirror | Added 4 new SQLAlchemy models BEFORE HtsHeatMirror: HCasterHeatProcessMirror, HCasterConsumptionMirror, HEqupBreakdownMirror, HUnitCodeMirror | HTS analytics roadmap Tier 1 needs caster heat lifecycle (~15.5K rows), consumption/yield (~15.5K rows), breakdown log (~800 rows), and unit-code lookup (36 rows) | Discovered during 27-schema deep audit (memory: project_oracle_schemas_visible.md). All HTS-side gold sat unused | Curated col subsets: HCasterHeatProcessMirror keeps 20 of 108 upstream cols (lifecycle times, operators, REMARKS); HCasterConsumptionMirror keeps 22 of 70 (yield_pct + 8 loss cols + tonnage variants + 3 equip-life cols); HEqupBreakdownMirror is full 8-col mirror with composite unique (unit_code, brk_date, reason) for re-sync dedupe; HUnitCodeMirror is 2-col lookup |
| 145 | 2026-05-13 02:30 | backend/alembic/versions/hts002_add_caster_breakdown_mirrors.py | (did not exist) | NEW migration ~155 LOC | DDL for the 4 new mirror tables | Required to match the ORM definition in #144 | down_revision='wbaudit001' (clean linear: c3e8d219a4b1 → v2dash001 → livetrack001 → wbaudit001 → hts002). HtsHeatMirror unchanged — `sms` column already exists, bug was in sync (#146). Migration creates tables only; init_db().create_all() will be redundant-no-op on BF4 once migration runs first |
| 146 | 2026-05-13 02:30 | backend/utils/hts_sync.py | `row_to_mirror_dict()` read `r.get("SMS")` — column doesn't exist upstream | Read `r.get("SMS_UNIT")` (with fallback to `r.get("SMS")` for forward-compat) | `hts_heat_mirror.sms` was NULL since launch because upstream column is named `SMS_UNIT` not `SMS` | Discovered during deep audit — sample row showed SMS_UNIT='SMS-2'. The code never crashed because `.get("SMS")` quietly returned None | Single-line fix + inline comment. Next 5-min HTS sync tick UPSERTs all existing rows, backfilling sms in-place |
| 147 | 2026-05-13 02:30 | backend/utils/hts_sync.py | run_once() synced one table (VW_HTS_HOTMETAL_DATA) and bailed on any error | Added 4 per-table sync functions (pull_caster_hp, pull_caster_cn, pull_breakdowns, pull_unit_codes) + a generic _generic_upsert helper + a 5-stage run_once() that runs all syncs on one connection/session with per-table try/except + _safe_rollback between them | One scheduler tick should now cover all 5 HTS tables, but a stale upstream table on table 3 must not abort the next 2 | Required by Tier 1 — SMS Performance + breakdown alerts need fresh caster + breakdown rows | Watermarks: hotmetal → MAX(torpedo_in_time); caster_hp → MAX(caster_date); caster_cn → joined to caster_hp's CASTER_DATE via Oracle-side JOIN; breakdowns → MAX(brk_date); unit_codes → full refresh (36 rows). Critical: each per-table `try` rolls back on error so PG's "InFailedSqlTransaction" doesn't cascade |
| 148 | 2026-05-13 02:30 | backend/utils/hts_sync.py | run_once() ended after sync | After breakdown sync, calls `scan_hts_breakdowns(db)` from alert_detector + commits if new alerts; scan failure rolls back and is logged but doesn't fail the breakdown sync | Equipment breakdowns should fold into the V2 Dashboard Alerts feed within the same sync cadence | Per HTS analytics roadmap Tier 1 #7 | Best-effort — if the scanner crashes we still keep the freshly-synced mirror row. `out["breakdowns"]["new_alerts"]` reported in the per-table stats |
| 149 | 2026-05-13 02:30 | backend/utils/alert_detector.py | 7 detector functions + 2 orchestrators (wbatngl + fleet) | Added `detect_hts_breakdown(row, unit_name)` + `scan_hts_breakdowns(db)` + a `_has_recent_breakdown_alert(db, message)` 12h-window dedupe helper | Equipment breakdowns from HTS need their own detector — unlike wbatngl detectors, breakdown rows aren't keyed to a torpedo so the existing (kind, torpedo_id) dedupe doesn't apply | Per HTS analytics roadmap Tier 1 #7 | Severity: high if reason contains TORPEDO/LADLE/BOF/CONVERTER, else by delay_type (DEL→med, OBM→low). Dedupe by exact `message` field within 12h since the message embeds unit_label + reason + brk_date and is therefore effectively unique. Last 24h scanned every tick — same row safely skipped on next 12h scan |
| 150 | 2026-05-13 02:30 | backend/routes/sms_performance.py | (did not exist) | NEW ~430 LOC route module with 3 endpoints under /api/sms-performance/v1/* | Backend for the new SMS Performance page | Per HTS analytics roadmap Tier 1 #4/#5 | `GET /overview?range=today\|shift_a\|7d\|30d` returns 5 KPIs (heats_total / avg_yield_pct / best_yield_pct / heats_below_target / total_loss_tons) + daily yield_trend + by_sms + by_shift + by_grade (top-8). `GET /loss-pareto` returns ordered category list (tonnes-class sorted desc by value, count-class trailing in definition order) + per-grade Pareto. `GET /heats` paginated detail with sms/shift/below-target filters. Yield target read from SystemConfig key `SMS_YIELD_TARGET_PCT` (default 96.0). All queries cooperatively join HCasterConsumption ↔ HCasterHeatProcess ↔ HtsHeatMirror via heat_no/heatno |
| 151 | 2026-05-13 02:30 | backend/routes/tracking_v2.py | torpedo_detail response had no HTS-side heat attribution | Added `_current_heat_for_fleet(db, fleet_id)` helper + new `current_heat` key in the detail response — pulls hts_heat_mirror row within 90 min and joins it to caster_hp (for grade/shift/operator) + caster_cn (for yield_pct) | Heat-to-Trip live map (Tier 1 #2) lets analysts see which heat a torpedo is currently feeding into | Per HTS analytics roadmap Tier 1 #2 — folds into the existing /torpedoes/{fleet_id} payload so no extra round-trip | 90-min window covers typical torpedo dwell + grace period after torpedo_out_time. Renders null when no recent HTS row exists — frontend hides the FED INTO section in that case |
| 152 | 2026-05-13 02:30 | backend/main.py | imports + registrations for V2 / Tracking V2 / WB Audit | Added `sms_performance` to import line + `app.include_router(sms_performance.router)` after weighbridge_audit | Wire new router | Standard pattern | Two-line edit |
| 153 | 2026-05-13 02:30 | frontend/src/pages/SMSPerformance.jsx | (did not exist) | NEW page container ~110 LOC | Hosts the 60s tick, range/sms-filter state, and HeaderContext for the right-side range pills | Per HTS analytics roadmap Tier 1 — sister page to WeighbridgeAudit | Default range='today'. HeaderContext.right injects [Shift A] [Today] [7d] [30d] pills. Master 60s tick; HTS sync runs every 5 min upstream so anything faster is wasted polling. Body layout: KPIRow top, then two grid-2col rows (YieldTrend+LossPareto, BySMSTable+filter-helper-card), then HeatsTable full-width. Click-to-filter glue lifts smsFilter from BySMSTable to HeatsTable via parent state |
| 154 | 2026-05-13 02:30 | frontend/src/components/SMSPerformance/* | (did not exist) | NEW components: KPIRow.jsx (~85 LOC), YieldTrend.jsx (~90 LOC), LossPareto.jsx (~110 LOC), BySMSTable.jsx (~80 LOC), HeatsTable.jsx (~135 LOC), SMSPerformance.css (~250 LOC) | All chart + table primitives for the new page | Per HTS analytics roadmap Tier 1 + design idea | Reuses Recharts (already in vendor-charts chunk). YieldTrend has a horizontal ReferenceLine at the target %; LossPareto separates tonnes-units (in chart) from count-units (chip strip below); HeatsTable does server-side pagination with optional sms/shift/below-target filters. CSS all scoped under `.smsperf` with private `--sp-*` token aliases, same isolation discipline as WeighbridgeAudit.css |
| 155 | 2026-05-13 02:30 | frontend/src/components/LiveTrackingV2/TorpedoDetailPanel.jsx | 7-section panel, no HTS-side heat | Inserted new `FED INTO` section (~3 lines wrapper) between CURRENT TRIP and CHEMISTRY & TEMP; added `FedInto` sub-component (~75 LOC) | Render the new `current_heat` payload from #151 | Per HTS analytics roadmap Tier 1 #2 | Section renders ONLY when `detail.current_heat` is non-null (i.e. HTS has a row for this torpedo in the last 90 min). FedInto shows AT → FEEDING converter, heat#, grade, shift, operator, yield (or "pouring" badge if heat still active), and torpedo_in/out times. Reuses ChevronRight icon already imported for CurrentTrip |
| 156 | 2026-05-13 02:30 | frontend/src/components/LiveTrackingV2/LiveTrackingV2.css | No `.v2-track-detail-fedinto*` classes | Added `.v2-track-detail-fedinto`, `-fedinto-meta`, `-fedinto-times`, `.v2-track-fedinto-pouring` ~30 LOC | Style the new FED INTO section + the "pouring" badge | Per #155 | Flex column with 6px gap; meta row uses dashed top border to separate from the route line; "pouring" badge uses the accent color tinted at 12% opacity — matches the pill vocabulary used elsewhere in the panel |
| 157 | 2026-05-13 02:30 | frontend/src/App.jsx + Sidebar.jsx | No SMS Performance route or sidebar entry | App.jsx: lazy `SMSPerformance` import + `'/sms-performance': { title: 'SMS Performance' }` in ROUTE_CONFIG + `<Route path="/sms-performance" .../>` inside isAdminOrTRS block. Sidebar.jsx: added `Gauge` to lucide import + inserted entry at position 5 in both admin and TRS arrays (slot below Weighbridge Audit) | Surface the new page in the sidebar nav, admin/TRS only | Per HTS analytics roadmap Tier 1 + user-approved sidebar slot 5 | PPC and Operator menus intentionally unchanged. Inline comment in Sidebar.jsx points future readers to the roadmap memory |
| 158 | 2026-05-13 02:30 | frontend build | Prior sprint sizes | `npx vite build` → ✓ built in 8.94s, new `SMSPerformance-D__bI4_u.js` chunk 12.18 kB / 3.40 kB gzip, all other chunks size-stable | Verify the new module + the TorpedoDetailPanel edit integrate cleanly | Sanity check before handover | No errors, no new lint regressions |
| 159 | 2026-05-13 10:35 | backend/database/models.py | `from sqlalchemy import Column, Integer, String, Float, Numeric, DateTime, Boolean, Date, UniqueConstraint, Index, ForeignKey` | Added `Text` to the import list (alphabetical between Date and UniqueConstraint) | BF4 `alembic upgrade head` failed at import time: `NameError: name 'Text' is not defined` in `HCasterHeatProcessMirror.remarks = Column(Text)` | My local `python -c "import ast; ast.parse(...)"` syntax check only verified parse-correctness, NOT name resolution. The two Text-typed columns (`remarks`, `liqui_robotic_remarks`) referenced a name that wasn't imported. `ast.parse` is not sufficient; `python -c "from backend.database import models"` is the actual smoke test | One-word edit; re-mirrored handover copy. Lesson noted: from now on, after adding any sqlalchemy `Column(<Type>)` of a new type, the verify step must be `python -c "from backend.database import models"`, NOT just `ast.parse` |
| 160 | 2026-05-13 11:00 | docs/plans/2026-05-13-heat-trace-design.md | (did not exist) | NEW design doc (~95 LOC) | Approved plan for Tier 2 #1 of the HTS analytics roadmap — end-to-end BF→torpedo→SMS→caster trace anchored by heat_no, surfaced as a drawer from SMS Performance heats table | User asked "are we using it" after Tier 1 deploy worked. The data was captured but no single view stitched it together. User said "yes go ahead" | Captures 5 decisions: drawer-not-page surface, heat-as-anchor (not trip), 6h BF lookback window, server-side pre-computed durations, read-only audit view |
| 161 | 2026-05-13 11:00 | backend/routes/heat_trace.py | (did not exist) | NEW ~280 LOC route module with 2 endpoints under /api/heat-trace/v1/* | Backend for the Heat Trace drawer | Per HTS analytics roadmap Tier 2 #1 | `GET /by-heat/{heat_no}` anchors at heat_no, finds matching WBATNGL trip via (torpedo + 6h backward window from torpedo_in_time), pulls caster process + consumption, builds chronological timeline (bf_tap → bf_exit → hts_in → ladle_on → ladle_open → ladle_close → hts_out), computes 4 gap durations server-side (transit_min / dwell_min / tap_to_cast_min / ladle_open_to_close_min). `GET /by-trip/{trip_id}` is the inverse: anchored at WBATNGL trip, finds heat(s) via 6h forward window; returns first heat as primary + `additional_heats` array if torpedo fed multiple converters. Both endpoints return `bf_side=null` (or `hts_arrival=null`) when the match window has no hit — that's a real condition, not an error |
| 162 | 2026-05-13 11:00 | backend/main.py | imports + registrations through `sms_performance` | Added `heat_trace` to the import line + `app.include_router(heat_trace.router)` after sms_performance | Wire new router | Standard pattern | Two-line edit |
| 163 | 2026-05-13 11:00 | frontend/src/components/SMSPerformance/HeatTraceDrawer.jsx | (did not exist) | NEW drawer component ~270 LOC | Renders /by-heat/{heat_no} payload in a 420px right-side slide-in panel | Per design doc | 7 sections top-to-bottom: header (heat_no + X), outcome strip (4 KPI-ish cells), 4 stage cards (BF tap / Torpedo trip / SMS-Caster / Losses), durations row (4 gap blocks), timeline (color-coded by stage kind), REMARKS. Backdrop click closes the drawer. fmtTime helper formats ISO timestamps to short "MMM dd HH:mm" form. Inline tone logic: yield<96 → amber, temp<1450 → amber, s_l>0.05 → amber. No state of its own beyond the API fetch — drawer un-mounts when parent clears traceHeatNo |
| 164 | 2026-05-13 11:00 | frontend/src/components/SMSPerformance/SMSPerformance.css | No drawer or trace styles | Appended ~250 LOC for `.smsperf-drawer*` + `.smsperf-trace-*` blocks: backdrop + slide-in panel (420px, max-width 92vw), header, body scroll, outcome grid (4-cell), stage cards (header + body), key-value rows, gap grid (4-cell), color-coded timeline (BF=indigo, WB=violet, SMS=accent, Caster=orange), remarks. Two CSS keyframes (fade-in backdrop, slide-in panel) | Style the new drawer | Per #163 | Scoped under `.smsperf*` namespace so it inherits the SMS Performance page's CSS variable aliases (text/bg/border/accent). Mobile breakpoint at 600px collapses 4-col grids to 2 |
| 165 | 2026-05-13 11:00 | frontend/src/pages/SMSPerformance.jsx + components/SMSPerformance/HeatsTable.jsx | HeatsTable rows were inert; no drawer was mounted from the page | SMSPerformance.jsx: import HeatTraceDrawer, add `traceHeatNo` useState, pass `onRowClick={setTraceHeatNo}` to HeatsTable, render `<HeatTraceDrawer heatNo={traceHeatNo} onClose={...} />` conditionally. HeatsTable.jsx: accept `onRowClick` prop, mark rows `smsperf-row-clickable`, fire `onRowClick(h.heat_no)` on click, added a "· click any row for full trace" hint to the card subheader | Wire clicks into the drawer | Per design doc | Drawer state lifted to the page so closing+reopening doesn't lose the table filter state. onRowClick is null-safe (drawer optional) so HeatsTable can be reused without it |
| 166 | 2026-05-13 11:00 | frontend build | Prior sprint sizes | `npx vite build` → ✓ built in 9.05s, SMSPerformance chunk grew from 12.18 kB → ~14 kB (adding HeatTraceDrawer + drawer CSS), all other chunks size-stable | Verify the new module integrates cleanly | Sanity check before handover | No errors |
| 167 | 2026-05-13 11:10 | backend/utils/hts_sync.py | `pull_breakdowns` upserted `mirror_rows` directly | Added a Python-side dedupe by `(unit_code, brk_date, reason)` before upsert (last-write-wins) and report `intra_batch_dups` count in stats | BF4 first run failed with `psycopg2.errors.CardinalityViolation: ON CONFLICT DO UPDATE command cannot affect row a second time` — upstream H_EQUP_BREAKDOWNS has duplicate (unit_code, brk_date, reason) tuples and Postgres's ON CONFLICT can only target one row per conflict key per INSERT statement | Postgres-native limitation — caller (us) must dedupe by conflict key when the source allows duplicates | The composite unique index still protects across batches; we only need to dedupe WITHIN a single batch. Dedup is a small dict pass; cost is negligible vs the network fetch |
| 168 | 2026-05-13 11:10 | backend/utils/hts_sync.py | `_CASTER_HP_COLS` and `_CASTER_CN_COLS` used bare `SEQUENCE`, `SHIFT`, `DELAY`, `YIELD` column names | Wrapped each of those in double-quotes (`"SEQUENCE"`, etc.) so Oracle's SQL parser can't be tempted to parse them as DDL keywords | BF4 first run: `caster_hp` returned 0 fetched despite empty mirror (watermark=epoch) and 15.5K upstream rows — possible cause is one of these non-reserved keywords parsing strangely on JSW's Oracle | Reserved-word defensiveness; bare names *should* work but quoting eliminates the possibility | Double-quoted identifiers in Oracle must match the case of the upstream column (all UPPERCASE for these). Same fix applied to consumption SQL since both share the SEQUENCE column |
| 169 | 2026-05-13 11:10 | backend/utils/hts_sync.py + test_hts_caster_probe.py | `pull_caster_hp` ran the WHERE-watermark query with no introspection logging; no standalone diagnostic | Added a one-shot upstream probe `SELECT COUNT(*), MAX(CASTER_DATE) FROM HTS.H_CASTER_HEAT_PROCESS` when watermark is the epoch floor; also NEW `test_hts_caster_probe.py` at repo root which runs 6 distinct probes (bare count / NULL distribution / our binding pattern / explicit TO_DATE / bare-vs-quoted SELECT / consumption count / breakdown dup-key count) | Need to debug why caster_hp returned 0 fetched on BF4 without redeploying multiple times | The probe is run-once per empty-mirror tick (no perf cost), AND a standalone CLI script lets the user gather the same data immediately without waiting for the next sync tick | Probe script reads `backend/.env` via python-dotenv if installed, falls back to OS env. Prints labelled sections so output is easy to paste back. NO side effects on the local DB — pure read-only against Oracle |
| 170 | 2026-05-13 11:15 | backend/utils/hts_sync.py | All 5 pull functions (`pull_and_upsert`, `pull_caster_hp`, `pull_caster_cn`, `pull_breakdowns`, `pull_unit_codes`) accepted a shared `cursor` arg; the orchestrator opened ONE `cur = conn.cursor()` and passed it to every pull in sequence | Changed signatures from `(db, cursor, ...)` to `(db, conn, ...)`; each pull opens a fresh cursor inside `try/finally` and closes it. Orchestrator no longer opens a cursor at all | BF4 probe (run #169) PROVED upstream is healthy: 15,575 rows in H_CASTER_HEAT_PROCESS, max CASTER_DATE = 2026-05-13 09:45:59, our exact `WHERE CASTER_DATE > :wm` returns 15,574 rows on a FRESH cursor. But the sync's caster_hp returned 0 fetched. The only remaining variable: cursor state leaked across multiple `cursor.execute(...)` calls within one run_once tick (oracledb apparently caches bind types or statement plans in a way that breaks 0-row vs N-row outcomes on subsequent re-executes) | Probe definitively isolated cursor reuse as the cause: same SQL, same bind value, same connection — fresh cursor → 15K rows; reused cursor → 0 rows | Fresh-cursor-per-pull also makes each pull self-contained for future testing in isolation. Cost is essentially zero — oracledb cursor creation is a thin handle. Also added DEBUG-level logging of `wm=<value>` immediately before each `cursor.execute` so future regressions are easier to spot |
| 171 | 2026-05-13 12:00 | frontend/src/components/Sidebar.jsx + App.jsx + pages/SMSPerformance.jsx + components/SMSPerformance/SMSPerformance.css | Page was labeled "SMS Performance" implying multi-SMS coverage | Renamed to "SMS-4 Performance" everywhere (Sidebar label both admin+TRS, ROUTE_CONFIG title, plus a subtle scope-note band above the KPI strip explaining the source table is SMS-4-only) | Direct join analysis on BF4 after hotmetal backfill proved `HTS.H_CASTER_HEAT_PROCESS` is purely SMS-4: 1,567 heats joined to SMS-4 hotmetal, ZERO joined to SMS-1/2/3 (the 33,562 SMS-2 hotmetal heats use a different upstream caster table, likely in SPT001A) | User chose Option A from the 3-path matrix: rename + label honestly rather than (B) add hotmetal-receipts panel or (C) audit SPT001A and add SMS-2 caster mirror | URL `/sms-performance` kept unchanged so existing bookmarks still work. Heat Trace drawer + Live Tracking V2 FED INTO section both read `hts_heat_mirror` directly, so they still show SMS-2/3 attribution for non-SMS-4 heats — only the page-level yield aggregates are SMS-4-restricted. Scope note styled subtle (hsl(214 32% 96%) band, 11.5px) so it informs without nagging |
| 172 | 2026-05-13 12:15 | docs (8 files) | `conda activate hmd_test` / `hmd_env` referenced as BF4 setup in handover READMEs, test scripts, and plan docs | All replaced with `cd C:\Users\v_subramanya.gopal\Desktop\HMD; .venv\Scripts\activate.bat` (BF4 flat layout, Python venv) | User confirmed 2026-05-13 that BF4 runs Python `.venv` ONLY — no conda — and the working path is `C:\Users\v_subramanya.gopal\Desktop\HMD` (no `Development\Version_07\` subfolder) | Old handover docs were authored when conda was the assumed env manager; later switched to plain venv after conda failed on JSW's SSL-inspected `defaults` channel but stale instructions were never cleaned | Files updated: `handover/2026-05-08-jsw-sync-now-fix/README.md`, `handover/2026-05-08-wbatngl-trip-mirror-sprint/README.md`, `handover/2026-05-07-live-tracking-sprint/README.md` (2 spots), `handover/2026-05-07-live-tracking-sprint/backend/utils/cleanup_fleet_location_dupes.py`, `handover/2026-05-11-hts-unblock/test_kotaiah_bf5_fix.py`, `Development/Version_07/test_kotaiah_bf5_fix.py`, `Development/Version_07/docs/plans/2026-05-08-wbatngl-trip-mirror-implementation.md`, `Development/Version_07/docs/plans/2026-04-16-strip-security-to-local-auth.md` (2 spots). Memory index `MEMORY.md` + `project_client_pc_context.md` also corrected. `app.bat:18` historical comment kept (factual note). No code-execution change |
| 173 | 2026-05-13 12:40 | backend/routes/v2_dashboard.py + frontend/src/components/Statistics/V2/KPIRow.jsx | Card 1 **HOT METAL DISPATCHED** used `closetime` (one stage earlier than BF gate exit) anchored on `_start_of_day_utc()` (UTC-naive, causing the "today" filter to silently absorb ~24h of yesterday's data between 00:00 IST and 05:30 IST every day). Sparkline left-padded missing-hour buckets so the time-axis collapsed whenever any hour had zero trips. Frontend sub-label said "shift to date · today" but the SQL was calendar-day, not shift-aligned. | (1) New helpers `_IST_OFFSET`, `_now_ist_naive()`, renamed `_start_of_day_utc` → `_start_of_day_ist` everywhere (matches `operations.py:43-49` pattern); `_hours_ago()` re-anchored on `_now_ist_naive()`. (2) Card 1 calc now uses `dispatch_ts = COALESCE(out_date, closetime)` as the anchor — `out_date` is the canonical BF gate-exit (see `jsw.py:30-49` stage map), with closetime fallback so newly-weighed rows aren't undercounted. (3) Sparkline rebuilt as a dense 24-bucket dict keyed on `date_trunc('hour', dispatch_ts)` → walked oldest→newest from `_now_ist_naive()` so every hour gets an entry (0 if no trips). (4) Sub-label changed to `"today · 00:00 IST onward"`. | User-driven systematic audit of V2 Dashboard, Card 1 of 14. User picked: window=calendar-day, semantics=Option A (BF-side dispatch), apply IST fix everywhere in this file, change misleading sub-label. | Helper-level timezone bug propagated from earlier UTC-anchored code; `closetime` choice predated the project's BF-exit anchor convention now used by `heat_trace.py` and `operations.py`. | Same `_IST_OFFSET / _now_ist_naive` pattern as `operations.py` Phase 2 hotfix (changes_tracker #67-71). `out_date` matches `heat_trace.py:73-76` and `operations.py:201`. Helper rename is safe — both helpers were only called from this single file. The sankey caller at line 490 was renamed in this commit but its full logic stays Card-9-scoped (audited later). Frontend chunk size stable; no UI other than the one sub-label string. `dispatch_ts` is constructed once outside the query as a SQLAlchemy `func.coalesce` expression so both the big-number SUM and the hourly GROUP BY share the same anchor — important for spark + total to stay consistent on every tick. Sparkline now always returns exactly 24 elements, indexed by hour offset from the current IST wall-clock hour. |
| 174 | 2026-05-13 12:55 | backend/routes/v2_dashboard.py | Card 2 **ACTIVE TRIPS** counted rows in V07's native `Trip` table (`status BETWEEN ASSIGNED..UNLOADING_ENDED, deleted_at IS NULL`) which is effectively empty on BF4 — operators never create manual trips, real trip data only flows in via the WBATNGL sync. Card stuck at `0` while the same dashboard's bottom Active Trips table (which reads `WbatnglTripMirror` correctly) showed 5-7 live rows, the Fleet Donut showed 6 Returning torpedoes, and the Sankey showed non-zero ribbons. | Replaced the Trip-table count with a WBATNGL-mirror in-flight count: `WbatnglTripMirror.out_date IS NOT NULL AND sms_ack_time IS NULL AND out_date >= (_now_ist_naive() - ACTIVE_TRIP_WINDOW_HOURS hours)`. Added module-level constant `ACTIVE_TRIP_WINDOW_HOURS = 6` (mirrors `operations.py:66`; documented as needing extraction if a 3rd callsite appears). `total_torpedoes` denominator unchanged — still `FleetManagement` gross count (53). Frontend sub-label "of {N} torpedoes" left as-is per user decision (Option A across all 3 ambiguity points). | User-driven systematic audit of V2 Dashboard, Card 2 of 14. User picked: Option A (in-flight, 6h bounded) for the "active" definition + keep gross 53 + keep current sub-label wording. | The KPI was wired against the wrong source table during the initial V2 dashboard scaffold; nobody noticed because BF4's Trip table really is empty so it never threw an error, just always returned 0. | Definition now matches `operations.py:201` exactly — single source of truth for "in-flight". 6h window prevents stuck/forgotten-ack rows from inflating the counter (operators occasionally skip the SMS ack). The kept-gross 53 denominator is the simpler, more familiar plant-wide fleet number; if we ever want "in-service" (excludes Hot Repair + Ign Off → 49 right now), we'd want a third KPI rather than overloading this sub-line. Imports unchanged — `Trip` and `TripStatus` are still used by `_active_torpedo_id_set` and `_fleet_breakdown` (Card 7, audited later). |
| 175 | 2026-05-13 13:08 | test_avg_cycle_probe.py (NEW) + handover/2026-05-13-card3-avg-cycle-probe/ | (no probe existed) | Read-only investigation script that runs the Card 3 SQL filter A (current `closetime >= today`) and filter B (candidate `sms_ack_time >= today`), reports count/min/max/mean/median/stddev + minute-bucket histogram + top 5 longest cycles + count of rows that crossed midnight. | Before patching Card 3 we needed evidence on whether the 397.1 min average was real or a few outliers dragging the mean. Probe answered both in 30 seconds: mean ≈ median (397.1 vs 379.4) → distribution is healthy, not outlier-inflated; A and B return identical row sets today but B is semantically cleaner. | Per user "investigate first" — we run probes before guessing. | Sample size today is small (16) but enough to confirm distribution shape. 9 of 16 trips crossed midnight (long overnight-queue waits) — that's a real plant pattern, not a data bug. Probe kept in repo for future Card 3 regressions; pattern matches `test_hts_caster_probe.py` (one-shot diagnostic). Commit `e8fdb91`. |
| 176 | 2026-05-13 13:15 | backend/routes/v2_dashboard.py + frontend/src/components/Statistics/V2/KPIRow.jsx | Card 3 **AVG CYCLE** filtered on `closetime >= start_today` (boundary-anchored on middle of the cycle); reported arithmetic mean (vulnerable to single overnight-queue outliers on future days); frontend sub-label `"tap → SMS ack"` is technically inaccurate — there's no "tap" timestamp in WBATNGL, the calc actually starts at `first_tare_time` (BF first weighbridge entry, pre-tap). | (1) Filter swapped: `sms_ack_time >= start_today` — anchored on cycle END so cross-midnight trips are counted honestly. (2) Reports `statistics.median(cycle_durations)` instead of arithmetic mean — robust to single 12h+ outliers without dropping data. JSON key kept as `avg_cycle_min` to avoid frontend payload churn (frontend label "AVG CYCLE" remains; "avg" understood as "typical"). (3) Sub-label `"tap → SMS ack"` → `"BF arrival → SMS ack"`. | User-driven systematic audit of V2 Dashboard, Card 3 of 14. After investigating with probe #175, user picked: filter B + median + "BF arrival → SMS ack" label. | Original filter A was likely the path-of-least-resistance choice (closetime is the timestamp set last in the BF-side weighbridge flow; easier to think of as "completed today"). Mean was the default Python idiom. "tap" label was informal plant-speak conflated with the actual data field. | Filter A and B return identical 16 rows today (verified by probe), but B is more future-proof for days when many trips weigh late yesterday and ack today. Median value will be slightly lower than mean on most days (379.4 vs 397.1 today, ~5% lower); operations may notice a one-time shift after deploy — communicate this. `import statistics` added to v2_dashboard.py imports. |
| 177 | 2026-05-13 13:25 | test_temp_drop_probe.py (NEW) + handover/2026-05-13-card4-temp-drop-probe/ | (no probe existed) | Read-only probe for Card 4 (TEMP DROP BF→SMS) — counts rows with `temp` vs `bds_temp` populated in 24h and 30d windows; per-source-lab + per-source-table breakdown; numeric range of `bds_temp` where non-NULL; sample rows with BOTH temps; BF-side `temp` sanity check. | Card showed 0 °C. Probe definitively answered: `bds_temp` has NEVER been populated in any synced row (0 / 2,314 over 30 days, 0 / 48 in 24h, 0% across all 6 BFs and both Oracle source-tables). BF-side `temp` is healthy (2,288 rows, 1387-1578 °C, mean 1488). | Per user "investigate first" — same probe-driven discipline as Card 3. | Pattern matches `test_avg_cycle_probe.py` (#175). Probe kept in repo for future regressions. Commit `c57f9e3`. |
| 178 | 2026-05-13 13:30 | backend/routes/v2_dashboard.py + frontend/src/components/Statistics/V2/KPIRow.jsx | Card 4 **TEMP DROP BF→SMS** subtracted `WbatnglTripMirror.bds_temp` from `WbatnglTripMirror.temp` over 24h. Probe #177 showed `bds_temp` is NEVER populated — JSW doesn't appear to capture SMS-receive temp anywhere we sync (HTS mirror has no temp columns either). Card always showed `0 °C` because of the empty-list `else 0` fallback. The original comment said "WBATNGL.temp − HTS.bds_temp via torpedo match" — but the code read both temps from the same WBATNGL table, and the intended HTS join was never built. | Card 4 **repurposed** as `BF TAP TEMP` showing `AVG(WbatnglTripMirror.temp)` over last 24h. The BF-side `temp` column IS well-populated (97% of last-24h rows, plausible 1387-1578 °C range) — meaningful KPI on its own (tap-temp drift signals BF furnace issues). Backend payload key renamed `avg_temp_drop_c` → `avg_bf_tap_temp_c` (intentional break — new value is wildly different from old, so the rename prevents any silent stale-consumer bug). Frontend label `TEMP DROP BF→SMS` → `BF TAP TEMP`; icon `ThermometerSnowflake` (cold-implying) → `Thermometer` (neutral); sub-label `"last 24h avg"` kept. | User-driven systematic audit of V2 Dashboard, Card 4 of 14. After probe #177 proved the SMS-side data doesn't exist, user picked Option B (repurpose) over A (hide), C (deeper Oracle probe), D (different KPI). | Original card was speculatively designed against `bds_temp` / `HTS_BDS_TEMP` upstream columns; the sync code (`wbatngl_trip_sync.py:170`) maps both candidates, neither ever returned data. JSW may simply not measure SMS-receive temperature. | If JSW ever exposes SMS-receive temp, we can resurrect the original "temp drop" KPI as a separate card without conflict. Mean is fine for tap temp (tight distribution, no long-tail outliers) — no need for median like Card 3. Expected value: ~1488 °C live, drifting ±5 °C based on furnace state. The icon change from snowflake → plain thermometer makes the card visually consistent with the new meaning. |
| 179 | 2026-05-13 13:55 | test_on_spec_probe.py (NEW) + handover/2026-05-13-card5-on-spec-probe/ | (no probe existed) | Read-only investigation probe for Card 5 (ON-SPEC %). 8 sections: coverage in 24h, current production calc, S/Si distribution stats, histograms, pass count at 5 candidate threshold sets, failure breakdown, today's off-spec rows, per-source comparison. | Probe revealed THREE significant findings: (1) Si data only present on 21/49 today's rows (42.9% coverage), (2) Si data ONLY comes from `source_lab = BF4` over 30 days (790 rows; zero from BF1/BF2/BF5/COREX1/COREX2), (3) 30-day S distribution has a long anomalous tail (59.7% of rows > 0.08, max 2.83) likely from non-BF4 sources with different unit/scale. Today's BF4-only 21 trips all pass proposed bounds (S≤0.05 AND 0.30≤Si≤1.20). 30-day pass rate at proposed bounds: 83.3% (vs 89.5% at current upper-only). | Per user "investigate first" — probe-driven discipline. | Pattern matches `test_avg_cycle_probe.py` (#175) and `test_temp_drop_probe.py` (#177). Commit `e0ba39e`. |
| 180 | 2026-05-13 14:05 | backend/routes/v2_dashboard.py + frontend/src/components/Statistics/V2/KPIRow.jsx + backend/database/init_db.py | Card 5 **ON-SPEC %** used hardcoded thresholds (S≤0.05, Si≤1.20 upper-only); no Si lower bound (cold-metal violations silently passing); no sample-size visibility (a "100%" on n=2 read the same as on n=200); empty-sample fallback returned `0%` (looked like "everything failed" when reality was "no data"). | (1) Three new SystemConfig keys auto-seeded by `init_db.py`: `SPEC_S_MAX=0.05`, `SPEC_SI_MIN=0.30`, `SPEC_SI_MAX=1.20`. JSW can tune in Strategic Planning → System Settings without redeploy. (2) Calc now applies all three bounds (`s_l ≤ S_max AND Si_min ≤ si_l ≤ Si_max`). (3) Returns `on_spec_pct = null` when sample_size=0 — frontend renders "N/A" instead of misleading "0%". (4) New payload fields `on_spec_sample_size` + `on_spec_thresholds` so the UI can display `n=N` and (later) tooltip the live thresholds. (5) Sub-label `"S & Si within band"` → `"S & Si in spec · BF4 only · n=N"` — honest about the BF4-only coverage scope. Imports `get_config` from `analytics_helpers`. | User-driven systematic audit of V2 Dashboard, Card 5 of 14. After probe #179, user picked Option A from a 4-option menu (apply proposed bounds + SystemConfig + BF4-only labeling + n=X + N/A handling). | Hardcoded thresholds + upper-only check were path-of-least-resistance defaults from the initial V2 scaffold. Si-low (the dominant failure mode, 11.4% of 30d rows) was silently passing. | Defaults match industry baseline AND BF4 30-day probe distribution (median S 0.27, median Si 0.49). Expected dashboard shift: today's 100% stays 100% (today's 21 BF4 trips all pass proposed bounds), but sub-label changes to show coverage explicitly. SystemConfig pattern is idempotent — re-runs of init_db on BF4 will only add missing rows. **Flagged for separate audit ticket** (memory: project_v2_dashboard_audit.md): why non-BF4 sources lack Si in WBATNGL, and the anomalous high-S tail (median 0.27, max 2.83) on those rows — likely upstream unit/scale issue, needs JSW DBA conversation. |
| 181 | 2026-05-13 14:15 | backend/routes/v2_dashboard.py + frontend/src/components/Statistics/V2/KPIRow.jsx + frontend/src/components/Statistics/Version2Dashboard.jsx + frontend/src/components/Statistics/Version2Dashboard.css | V2 dashboard KPI row had 6 cards; middle row had 3 panels (FleetDonut · ThroughputChart · FlowSankey). User decided ON-SPEC + CHEM ALERTS + Producer→Consumer Sankey are not needed for the production dashboard — Cards 5, 6, 9 dropped immediately after / despite the Card 5 fix shipping (commit d214340). | (1) `KPIRow.jsx`: removed ON-SPEC + CHEM ALERTS `<KPICard>` blocks; removed `CheckCircle2` + `AlertTriangle` lucide imports; updated docstring to "4 cards". (2) `Version2Dashboard.css`: `.v2-kpi-grid` grid changed from `repeat(6,1fr)` → `repeat(4,1fr)`; responsive breakpoints rebalanced for 4-card layout (4 → 2 → 1); `.v2-row-middle` changed from `1.05fr 1.45fr 1.35fr` (3 cols) → `1fr 2fr` (2 cols, donut keeps slot, throughput absorbs Sankey's width). (3) `Version2Dashboard.jsx`: removed `FlowSankey` import and JSX usage; updated docstring. (4) `v2_dashboard.py`: removed on-spec calc, chem-alerts calc, and their payload fields (`on_spec_pct`, `on_spec_sample_size`, `on_spec_thresholds`, `chem_alerts_total`, `cold_count`, `chem_count`); removed `/sankey` route and its handler; cleaned up now-unused imports (`get_config`, `and_`, `case`, `or_`). | User decision — "drop these cards, no need". Production-ready dashboard with only KPIs that are real, plant-wide, and signal-bearing. | ON-SPEC was structurally BF4-only (Si data not synced for other 5 BFs); CHEM ALERTS had a known display bug (big number didn't match sub-line). Sankey was a 3rd middle-row card the user no longer wants. | (1) SystemConfig keys SPEC_S_MAX/SPEC_SI_MIN/SPEC_SI_MAX remain seeded — harmless, useful if cards return later. (2) Component files left intact: `KPICard.jsx` still used by other 3 KPIs; `FlowSankey.jsx` orphaned but kept for easy resurrection (it's ~scaled SVG that can drop back in). (3) Backend Alert ORM model still imported & used by the `/active-trips` and `/alerts` endpoints (bottom-row Alerts feed) — unaffected. (4) BF4 just-deployed Card 5 fix (d214340) will appear briefly until the user pulls #181 and rebuilds frontend — communicate this so they don't expect to see ON-SPEC. (5) No SystemConfig deletion needed; pattern is "additive, never destructive". |
| 182 | 2026-05-13 14:25 | test_fleet_donut_probe.py (NEW) + handover/2026-05-13-card7-fleet-donut-probe/ | (no probe existed) | Read-only probe for Card 7 (Torpedo Fleet Status donut). 5 sections: FleetManagement.status distribution; MaintenanceSchedule usage; per-torpedo bucket assignment under current vs proposed rules; side-by-side bucket count comparison; edge cases. | Validated the proposed 3-bucket classifier before patching. Probe confirmed: MaintenanceSchedule table is empty (0 rows ever — Hot Repair bucket is dead); 6 torpedoes correctly classified as ACTIVE under proposed rules (was 2 Returning + 4 hidden as Idle); all 53 fleet members have FleetLiveLocation rows; 1 anomaly worth auditing (TLC-36 has in-flight WBATNGL trip while in Maintenance — likely mid-trip breakdown). | Per user "probe first" workflow — same discipline as Cards 3, 4, 5. | Commit `fc150b5`. |
| 183 | 2026-05-13 14:30 | backend/routes/v2_dashboard.py + frontend/src/components/Statistics/V2/FleetDonut.jsx + test_stale_trip_audit.py (NEW) | Card 7 **Torpedo Fleet Status donut** had 7 buckets (Loading / In Transit / At SMS / Returning / Idle / Hot Repair / Ign Off); 3 always showed 0 because the trip-stage classifier read V07's empty manual `Trip` table (same root cause as Card 2). MaintenanceSchedule lookup required for Hot Repair was never populated on BF4 — bucket was structurally dead. `today = datetime.utcnow().date()` was UTC-anchored (same TZ class as Card 1's BUG B). The donut undercounted ACTIVE torpedoes by ~4 (those parked at SMS waiting to unload were silently bucketed as Idle). | (1) `_fleet_breakdown` rewritten as 3-bucket classifier matching Card 2's "in-flight" definition: `MAINTENANCE / ACTIVE / IDLE`, first match wins. (2) Dropped dependency on V07 Trip table and MaintenanceSchedule. (3) IST timezone bug moot — no date comparison left. (4) Dead helpers removed: `_classify_torpedo` (45 LOC), `_active_torpedo_id_set` (8 LOC). (5) `MaintenanceSchedule` import dropped from models import list. (6) `FleetDonut.jsx` SEGMENTS list reduced from 7 → 3 with new colors (ACTIVE green / IDLE gray / MAINTENANCE amber). (7) NEW companion probe `test_stale_trip_audit.py` — finds torpedoes with open WBATNGL trips while status=Maintenance (the TLC-36 pattern), dumps full trip + location + HTS-match details, recommends recovery option. | User-driven systematic audit of V2 Dashboard, Card 7 of 14. After probe #182a, user picked: 3 buckets (Option D), 6h in-flight window (same as Card 2), drop MaintenanceSchedule entirely, audit the TLC-36 stale-trip anomaly. | Original 7-bucket design predated the realization that V07 Trip + MaintenanceSchedule are empty on BF4. The OR-rule (`in-flight WBATNGL trip OR FM.status=Moving`) correctly handles all real-world cases including "torpedo at SMS waiting to unload" (in-flight trip exists, not Moving) and "torpedo returning empty after unload" (no trip record, FM.status=Moving). | Expected donut state on BF4 after deploy: 4 MAINTENANCE / 6 ACTIVE / 43 IDLE (vs current 0/0/0/2/47/0/4). TLC-36 wins MAINTENANCE rule (first-match) so the stale in-flight trip doesn't double-count. Stale-trip audit script preserved as `test_stale_trip_audit.py` for ongoing data-hygiene checks; recommendation in the script: Option C (do nothing — Card 2's 6h cap naturally expires stale rows after 6h). |
